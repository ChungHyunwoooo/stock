---
phase: 02-backtest-quality-gates
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - engine/backtest/multi_symbol.py
  - tests/test_multi_symbol.py
autonomous: true
requirements: [BT-03]

must_haves:
  truths:
    - "상관계수 |r| < 0.5 인 비상관 심볼 2-3개를 자동 선택할 수 있다"
    - "선택된 심볼들에 병렬 백테스트를 실행하고 심볼별 Sharpe를 얻는다"
    - "전체 심볼 Sharpe 중앙값 >= 0.5 이면 통과, 미만이면 실패"
  artifacts:
    - path: "engine/backtest/multi_symbol.py"
      provides: "MultiSymbolValidator — 상관계수 기반 심볼 선택 + 병렬 백테스트 + 중앙 Sharpe 게이트"
      exports: ["MultiSymbolValidator", "select_uncorrelated_symbols", "MultiSymbolResult"]
    - path: "tests/test_multi_symbol.py"
      provides: "멀티심볼 검증 테스트"
      min_lines: 60
  key_links:
    - from: "engine/backtest/multi_symbol.py"
      to: "engine/backtest/runner.py"
      via: "BacktestRunner 인스턴스 생성 + run() 호출 (ProcessPoolExecutor worker 내부)"
      pattern: "BacktestRunner\\(\\)"
    - from: "engine/backtest/multi_symbol.py"
      to: "engine/backtest/slippage.py"
      via: "SlippageModel 전달 (worker에 serializable args로 전달)"
      pattern: "slippage_model|NoSlippage"
---

<objective>
멀티심볼 병렬 백테스트 + 상관계수 기반 비상관 심볼 선택 + 중앙 Sharpe 기준 안정성 검증

Purpose: 단일 심볼 과적합을 방지하기 위해 비상관 심볼들에서 전략의 일관된 성과를 검증한다.
Output: multi_symbol.py, test_multi_symbol.py
</objective>

<execution_context>
@/home/hwchung/.claude/get-shit-done/workflows/execute-plan.md
@/home/hwchung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backtest-quality-gates/02-CONTEXT.md
@.planning/phases/02-backtest-quality-gates/02-RESEARCH.md
@.planning/phases/02-backtest-quality-gates/02-01-SUMMARY.md

<interfaces>
From engine/backtest/runner.py (modified by plan 01):
```python
class BacktestRunner:
    def __init__(self, slippage_model: SlippageModel | None = None, fee_rate: float = 0.0): ...
    def run(self, strategy, symbol, start, end, timeframe="1d",
            initial_capital=10_000.0, market=None, regime_enabled=False) -> BacktestResult: ...
```

From engine/backtest/parallel_optimizer.py (기존 패턴 참조):
```python
def _run_single(args: tuple) -> OptimizationResult | None:
    """ProcessPoolExecutor용 top-level 함수 — worker 내부에서 BacktestRunner 생성."""
    ...

class ParallelOptimizer:
    def __init__(self, n_workers: int = 4): ...
```

From engine/backtest/slippage.py (created by plan 01):
```python
class SlippageModel(Protocol): ...
class NoSlippage: ...
class VolumeAdjustedSlippage: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 상관계수 기반 심볼 선택 + MultiSymbolResult 정의 + 테스트</name>
  <files>engine/backtest/multi_symbol.py, tests/test_multi_symbol.py</files>
  <behavior>
    - select_uncorrelated_symbols returns list of symbols where all pairwise |corr| < 0.5
    - With perfectly correlated symbols (same returns), selects only 1
    - With uncorrelated symbols (independent random), selects up to n_select
    - MultiSymbolResult contains per-symbol Sharpe, median Sharpe, passed flag
    - median_sharpe >= 0.5 => passed=True
  </behavior>
  <action>
    1. `engine/backtest/multi_symbol.py` (전반부):
       - select_uncorrelated_symbols(symbols, returns_df, max_corr=0.5, n_select=3) -> list[str]
         - returns_df.corr()로 상관계수 행렬 계산
         - greedy 선택: 첫 심볼 포함, 이후 이미 선택된 모든 심볼과 |corr| < max_corr인 심볼만 추가
         - RESEARCH.md 코드 예시 참조

       - @dataclass MultiSymbolResult: symbols (list[str]), sharpe_per_symbol (dict[str, float]), median_sharpe (float), passed (bool), threshold (float)

    2. `tests/test_multi_symbol.py` (전반부):
       - 완전 상관 데이터 -> 1개만 선택
       - 독립 랜덤 데이터 (seed=42, 3 symbols) -> 3개 선택
       - max_corr=1.0 -> 전부 선택
       - MultiSymbolResult 생성 + passed 로직 테스트
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_multi_symbol.py -x -v -k "select or result"</automated>
  </verify>
  <done>상관계수 기반 심볼 선택이 |r| < 0.5 기준으로 비상관 심볼을 골라내고, MultiSymbolResult가 median Sharpe 기반 통과 여부를 판정</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MultiSymbolValidator 병렬 백테스트 + 통합 테스트</name>
  <files>engine/backtest/multi_symbol.py, tests/test_multi_symbol.py</files>
  <behavior>
    - MultiSymbolValidator.validate() runs backtest on each symbol via ProcessPoolExecutor
    - Returns MultiSymbolResult with per-symbol Sharpe and median
    - Worker function is top-level (pickle-safe), creates BacktestRunner inside worker
    - Only serializable args passed to worker (strategy dict, symbol str, dates str, fee float)
    - If a symbol backtest fails, it's skipped with warning (not fatal)
  </behavior>
  <action>
    1. `engine/backtest/multi_symbol.py` (후반부):
       - _run_symbol_backtest(args: tuple) -> tuple[str, float | None] — top-level 함수 (ProcessPoolExecutor용)
         - args = (strategy_dict, symbol, start, end, timeframe, initial_capital, fee_rate)
         - worker 내부에서 StrategyDefinition.model_validate(strategy_dict), BacktestRunner(fee_rate=fee_rate) 생성
         - runner.run() 호출, Sharpe 반환
         - 실패 시 None 반환 (logger.debug로 기록)
         - NOTE: SlippageModel은 pickle 불가할 수 있으므로 worker 내부에서는 NoSlippage 사용하거나 fee_rate만 전달. RESEARCH.md Pitfall 6 참조.

       - MultiSymbolValidator 클래스:
         - __init__(n_workers=4, sharpe_threshold=0.5)
         - validate(strategy, symbols, start, end, timeframe="1d", initial_capital=10_000.0, fee_rate=0.0) -> MultiSymbolResult
         - ProcessPoolExecutor(max_workers=n_workers)로 _run_symbol_backtest 병렬 실행
         - 성공 심볼의 Sharpe 수집, median 계산, threshold 비교
         - ParallelOptimizer 패턴 재사용 (as_completed, logging)

    2. `tests/test_multi_symbol.py` (후반부):
       - Mock 사용: BacktestRunner.run을 patch하여 심볼별 고정 Sharpe 반환
       - 또는 간단한 strategy + 짧은 기간으로 실제 실행 (느릴 수 있으므로 mock 권장)
       - median >= 0.5 통과 테스트
       - median < 0.5 실패 테스트
       - 일부 심볼 실패 시에도 나머지로 판정 테스트
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_multi_symbol.py -x -v</automated>
  </verify>
  <done>MultiSymbolValidator가 ProcessPoolExecutor로 심볼별 병렬 백테스트 실행하고, 중앙 Sharpe >= 0.5 기준으로 전략 안정성 판정</done>
</task>

</tasks>

<verification>
```bash
.venv/bin/python -m pytest tests/test_multi_symbol.py -x -v
```
기존 테스트 회귀:
```bash
.venv/bin/python -m pytest tests/ -x -q
```
</verification>

<success_criteria>
1. select_uncorrelated_symbols가 |corr| < 0.5 기준으로 비상관 심볼 선택
2. MultiSymbolValidator.validate()가 ProcessPoolExecutor 병렬 백테스트 실행
3. 중앙 Sharpe >= 0.5 이면 passed=True (CONTEXT.md locked)
4. 심볼 백테스트 실패 시 skip + warning (전체 실패 아님)
5. 기존 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-backtest-quality-gates/02-04-SUMMARY.md`
</output>
