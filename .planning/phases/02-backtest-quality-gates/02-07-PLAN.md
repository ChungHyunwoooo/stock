---
phase: 02-backtest-quality-gates
plan: 07
type: execute
wave: 4
depends_on: [02-05, 02-06]
files_modified:
  - api/routers/backtests.py
  - engine/interfaces/discord/commands/backtest_history.py
  - engine/backtest/history_cli.py
  - tests/test_backtest_interfaces.py
autonomous: true
requirements: [BT-04]

must_haves:
  truths:
    - "API GET /backtests/{strategy_id}/history 엔드포인트가 시간순 이력을 JSON으로 반환한다"
    - "CLI 커맨드가 전략별 이력을 rich table로 출력한다"
    - "Discord 슬래시 커맨드로 백테스트 이력을 조회할 수 있다"
    - "세 인터페이스 모두 BacktestRepository의 동일한 메서드를 사용한다"
  artifacts:
    - path: "api/routers/backtests.py"
      provides: "GET /backtests/{strategy_id}/history + DELETE 엔드포인트 확장"
      contains: "history"
    - path: "engine/interfaces/discord/commands/backtest_history.py"
      provides: "Discord 슬래시 커맨드 -- /백테스트이력"
      exports: ["BacktestHistoryPlugin"]
    - path: "engine/backtest/history_cli.py"
      provides: "CLI rich table 출력 -- 전략별 이력 + 전략 간 비교"
      exports: ["show_history", "compare_strategies"]
    - path: "tests/test_backtest_interfaces.py"
      provides: "API + CLI 통합 테스트"
      min_lines: 50
  key_links:
    - from: "api/routers/backtests.py"
      to: "engine/core/repository.py"
      via: "BacktestRepository.get_history() + compare_strategies()"
      pattern: "_repo\\.get_history|_repo\\.compare_strategies"
    - from: "engine/backtest/history_cli.py"
      to: "engine/core/repository.py"
      via: "BacktestRepository.get_history() + compare_strategies()"
      pattern: "BacktestRepository\\(\\)"
    - from: "engine/interfaces/discord/commands/backtest_history.py"
      to: "engine/core/repository.py"
      via: "BacktestRepository.get_history()"
      pattern: "BacktestRepository\\(\\)"
---

<objective>
백테스트 이력 조회/관리를 CLI(rich table) + API + Discord 커맨드로 제공

Purpose: CONTEXT.md locked "인터페이스: CLI(rich table) + API(/backtests/{strategy_id}/history) + Discord 커맨드 모두 제공" + "DB 관리: 삭제/수정/초기화 기능 CLI + API + Discord 모두 제공" 요구사항 충족. Plan 02-05에서 구현한 Repository 메서드를 세 인터페이스에서 동일하게 사용한다.
Output: 확장된 backtests.py, 신규 backtest_history.py (Discord), 신규 history_cli.py, 테스트
</objective>

<execution_context>
@/home/hwchung/.claude/get-shit-done/workflows/execute-plan.md
@/home/hwchung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backtest-quality-gates/02-CONTEXT.md
@.planning/phases/02-backtest-quality-gates/02-RESEARCH.md
@.planning/phases/02-backtest-quality-gates/02-05-SUMMARY.md
@.planning/phases/02-backtest-quality-gates/02-06-SUMMARY.md

<interfaces>
From engine/core/repository.py (modified by plan 05):
```python
class BacktestRepository:
    def save(self, session, record) -> BacktestRecord: ...
    def get(self, session, backtest_id) -> BacktestRecord | None: ...
    def get_by_strategy(self, session, strategy_id) -> list[BacktestRecord]: ...
    def list_all(self, session) -> list[BacktestRecord]: ...
    # NEW from plan 05:
    def get_history(self, session, strategy_id, limit=100) -> list[BacktestRecord]: ...
    def compare_strategies(self, session, strategy_ids: list[int]) -> list[BacktestRecord]: ...
    def delete(self, session, backtest_id: int) -> None: ...
    def delete_by_strategy(self, session, strategy_id: int) -> None: ...
```

From api/routers/backtests.py (기존):
```python
router = APIRouter(prefix="/backtests", tags=["backtests"])
_repo = BacktestRepository()

class BacktestResponse(BaseModel):
    id: int
    strategy_id: int
    symbol: str
    # ... existing fields

@router.get("/strategy/{strategy_id}", response_model=list[BacktestResponse])
def get_by_strategy(...): ...
```

From engine/interfaces/discord/commands/base.py:
```python
class DiscordCommandPlugin(Protocol):
    name: str
    def register(self, tree: app_commands.CommandTree, context: DiscordBotContext) -> None: ...
```

From engine/core/database.py:
```python
def get_session() -> Generator[Session, None, None]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API 이력/삭제 엔드포인트 + CLI rich table + 테스트</name>
  <files>api/routers/backtests.py, engine/backtest/history_cli.py, tests/test_backtest_interfaces.py</files>
  <behavior>
    - GET /backtests/{strategy_id}/history?limit=100 returns time-ordered BacktestResponse list
    - GET /backtests/compare?strategy_ids=1,2,3 returns cross-strategy comparison list
    - DELETE /backtests/{backtest_id} deletes a single record, returns 204
    - DELETE /backtests/strategy/{strategy_id} deletes all records for strategy, returns 204
    - show_history(strategy_id, limit) prints rich table to stdout
    - compare_strategies(strategy_ids) prints comparison rich table to stdout
    - CLI functions return list[dict] for testability (print is side effect)
  </behavior>
  <action>
    1. `api/routers/backtests.py` 확장 -- 기존 라우트 유지, 아래 추가:
       - GET /backtests/{strategy_id}/history: _repo.get_history(db, strategy_id, limit) 호출, BacktestResponse 리스트 반환. limit query param (default 100).
       - GET /backtests/compare: strategy_ids query param (comma-separated int list), _repo.compare_strategies(db, strategy_ids) 호출. 각 전략의 최근 백테스트 결과 반환.
       - DELETE /backtests/{backtest_id}: _repo.delete(db, backtest_id) 호출, 204 반환. 없으면 404.
       - DELETE /backtests/strategy/{strategy_id}: _repo.delete_by_strategy(db, strategy_id) 호출, 204 반환.
       - BacktestResponse에 Phase 2 신규 칼럼 추가: slippage_model, fee_rate, wf_result, cpcv_mode (from_record 업데이트)
       - NOTE: 기존 GET /strategy/{strategy_id}와 경로 충돌 방지 -- /backtests/{strategy_id}/history는 별도 경로이므로 충돌 없음.

    2. `engine/backtest/history_cli.py`:
       - show_history(strategy_id: int, limit: int = 20) -> list[dict]:
         - get_session()으로 DB 조회, get_history() 호출
         - rich.table.Table로 포맷: 날짜, 심볼, total_return, sharpe, max_dd, slippage_model, fee_rate, wf_result
         - rich.console.Console().print(table)
         - 테스트용으로 dict list도 반환
       - compare_strategies(strategy_ids: list[int]) -> list[dict]:
         - compare_strategies() 호출, rich table로 전략별 최근 결과 비교 출력
       - delete_history(strategy_id: int | None = None, backtest_id: int | None = None) -> None:
         - strategy_id면 delete_by_strategy, backtest_id면 delete
       - 이 파일은 `python -m engine.backtest.history_cli` 또는 import 모두 가능하도록 if __name__ == "__main__" 블록 추가 (argparse로 --strategy-id, --compare, --delete 등)

    3. `tests/test_backtest_interfaces.py`:
       - API 테스트: FastAPI TestClient 사용
         - GET /backtests/{strategy_id}/history 200 + 시간순 정렬 확인
         - GET /backtests/compare?strategy_ids=1,2 200
         - DELETE /backtests/{id} 204
         - DELETE /backtests/{non_existent_id} 404
       - CLI 테스트: show_history, compare_strategies 반환값 dict list 검증
       - in-memory SQLite + 테스트 레코드 fixture 사용
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_backtest_interfaces.py -x -v</automated>
  </verify>
  <done>API, CLI 인터페이스에서 백테스트 이력 조회/비교/삭제가 동작. BacktestRepository 동일 메서드를 공유.</done>
</task>

<task type="auto">
  <name>Task 2: Discord 슬래시 커맨드 -- 백테스트 이력 조회/관리</name>
  <files>engine/interfaces/discord/commands/backtest_history.py</files>
  <action>
    1. `engine/interfaces/discord/commands/backtest_history.py`:
       - BacktestHistoryPlugin 클래스 (DiscordCommandPlugin 프로토콜 구현)
       - name = "backtest_history"
       - register(tree, context) 메서드에서 슬래시 커맨드 등록:

       - /백테스트이력 strategy_id:int limit:int=10
         - BacktestRepository.get_history() 호출
         - discord.Embed로 테이블 형태 출력 (날짜, 심볼, 수익률, Sharpe, WF결과)
         - 결과 없으면 "이력이 없습니다" 메시지

       - /백테스트비교 strategy_ids:str (comma-separated)
         - BacktestRepository.compare_strategies() 호출
         - 전략별 최근 결과 비교 Embed

       - /백테스트삭제 backtest_id:int
         - BacktestRepository.delete() 호출
         - 확인 메시지 출력

       - 기존 Discord 커맨드 패턴(lifecycle.py, scanner.py) 참조
       - get_session() 컨텍스트 매니저 사용
       - 에러 처리: DB 조회 실패 시 사용자 friendly 메시지

    2. control_bot.py에서 BacktestHistoryPlugin을 import하고 plugins 리스트에 추가. 기존 플러그인 등록 패턴 참조.
       - 파일 수정 최소화: plugins 리스트에 BacktestHistoryPlugin() 한 줄 추가만.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from engine.interfaces.discord.commands.backtest_history import BacktestHistoryPlugin; p = BacktestHistoryPlugin(); assert p.name == 'backtest_history'; print('OK')"</automated>
  </verify>
  <done>Discord 슬래시 커맨드 /백테스트이력, /백테스트비교, /백테스트삭제가 등록 가능. CONTEXT.md "인터페이스: Discord 커맨드 제공" 충족.</done>
</task>

</tasks>

<verification>
```bash
.venv/bin/python -m pytest tests/test_backtest_interfaces.py -x -v
.venv/bin/python -c "from engine.interfaces.discord.commands.backtest_history import BacktestHistoryPlugin; print('import OK')"
```
기존 테스트 회귀:
```bash
.venv/bin/python -m pytest tests/ -x -q
```
</verification>

<success_criteria>
1. API GET /backtests/{strategy_id}/history 시간순 이력 반환
2. API GET /backtests/compare 전략 간 횡단 비교 반환
3. API DELETE 엔드포인트로 레코드 삭제
4. CLI rich table로 이력 출력 + 비교 출력
5. Discord /백테스트이력, /백테스트비교, /백테스트삭제 커맨드 등록
6. 세 인터페이스 모두 BacktestRepository 동일 메서드 사용
7. 기존 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-backtest-quality-gates/02-07-SUMMARY.md`
</output>
