---
phase: 02-backtest-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/backtest/slippage.py
  - engine/backtest/fee_model.py
  - engine/backtest/runner.py
  - engine/backtest/validation_result.py
  - engine/data/depth_cache.py
  - engine/data/depth_collector.py
  - config/exchange_fees.json
  - tests/test_slippage.py
  - tests/test_backtest_costs.py
autonomous: true
requirements: [BT-01]

must_haves:
  truths:
    - "VolumeAdjustedSlippage를 적용하면 동일 전략의 수익률이 NoSlippage 대비 낮아진다"
    - "거래소별 maker/taker 수수료가 JSON 설정에서 로드되어 백테스트에 적용된다"
    - "SlippageModel을 교체해도 BacktestRunner의 나머지 로직은 변경 없이 동작한다"
    - "DepthCache가 Parquet에서 심볼별 depth 통계를 읽어 VolumeAdjustedSlippage에 공급한다"
    - "OrderbookDepthCollector가 ccxt REST로 depth 스냅샷을 수집하여 Parquet에 저장한다"
  artifacts:
    - path: "engine/backtest/slippage.py"
      provides: "SlippageModel Protocol + NoSlippage + VolumeAdjustedSlippage"
      exports: ["SlippageModel", "NoSlippage", "VolumeAdjustedSlippage"]
    - path: "engine/backtest/fee_model.py"
      provides: "FeeModel -- JSON 설정 로더, maker/taker 수수료 계산"
      exports: ["FeeModel", "load_exchange_fees"]
    - path: "engine/backtest/validation_result.py"
      provides: "WindowResult + ValidationResult -- WF/CPCV 공유 인터페이스"
      exports: ["WindowResult", "ValidationResult"]
    - path: "engine/data/depth_cache.py"
      provides: "DepthCache -- Parquet read/write for depth statistics"
      exports: ["DepthCache"]
    - path: "engine/data/depth_collector.py"
      provides: "OrderbookDepthCollector -- ccxt REST fetch_order_book + Parquet cache write"
      exports: ["OrderbookDepthCollector"]
    - path: "config/exchange_fees.json"
      provides: "거래소별 수수료 설정"
      contains: "binance"
  key_links:
    - from: "engine/backtest/runner.py"
      to: "engine/backtest/slippage.py"
      via: "SlippageModel protocol injection in __init__ and _simulate"
      pattern: "self\\._slippage_model\\.calculate_slippage"
    - from: "engine/backtest/runner.py"
      to: "engine/backtest/fee_model.py"
      via: "fee_rate parameter in __init__ and _simulate"
      pattern: "self\\._fee_rate"
    - from: "engine/backtest/slippage.py"
      to: "engine/data/depth_cache.py"
      via: "VolumeAdjustedSlippage.__init__(depth_cache) -> get_stats(symbol)"
      pattern: "self\\._depth_cache\\.get_stats"
    - from: "engine/data/depth_collector.py"
      to: "engine/data/depth_cache.py"
      via: "OrderbookDepthCollector writes snapshots that DepthCache reads"
      pattern: "DepthCache|_cache_dir"
---

<objective>
SlippageModel 프로토콜 + DepthCache/DepthCollector + VolumeAdjustedSlippage 구현 + FeeModel + BacktestRunner 통합 + 공유 ValidationResult 인터페이스 정의

Purpose: 백테스트가 실매매 비용(슬리피지 + 수수료)을 반영하여 과대 성과 추정을 방지한다. Orderbook depth 데이터 파이프라인(수집 + 캐시)을 구축하여 VolumeAdjustedSlippage가 실제 depth 기반으로 동작하도록 한다. 또한 후속 plan에서 사용할 ValidationResult 공유 인터페이스를 정의한다.
Output: slippage.py, fee_model.py, validation_result.py, depth_cache.py, depth_collector.py, 수정된 runner.py, exchange_fees.json, 테스트 파일 2개
</objective>

<execution_context>
@/home/hwchung/.claude/get-shit-done/workflows/execute-plan.md
@/home/hwchung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backtest-quality-gates/02-CONTEXT.md
@.planning/phases/02-backtest-quality-gates/02-RESEARCH.md

<interfaces>
<!-- 기존 코드 -- executor가 직접 참조해야 할 인터페이스 -->

From engine/backtest/runner.py:
```python
@dataclass
class BacktestResult:
    symbol: str
    timeframe: str
    start_date: str
    end_date: str
    initial_capital: float
    final_capital: float
    total_return: float
    sharpe_ratio: float | None
    max_drawdown: float | None
    trades: list[TradeRecord] = field(default_factory=list)
    equity_curve: pd.Series = field(default_factory=pd.Series)

class BacktestRunner:
    def __init__(self) -> None: ...
    def run(self, strategy, symbol, start, end, timeframe="1d",
            initial_capital=10_000.0, market=None, regime_enabled=False) -> BacktestResult: ...
    def _simulate(self, df, initial_capital, exposure_series=None) -> tuple[pd.Series, list[TradeRecord]]: ...
```

From engine/backtest/metrics.py:
```python
def compute_total_return(equity_curve: pd.Series) -> float: ...
def compute_sharpe_ratio(equity_curve: pd.Series, periods_per_year: int = 252) -> float | None: ...
def compute_max_drawdown(equity_curve: pd.Series) -> float | None: ...
def compute_win_rate(pnls: list[float]) -> float: ...
def compute_profit_factor(pnls: list[float]) -> float: ...
```

From engine/data/ohlcv_cache.py:
```python
class ParquetCache:
    def __init__(self, cache_dir: Path, ttl_hours: int = 24): ...
    def get(self, symbol, start, end, timeframe) -> pd.DataFrame | None: ...
    def put(self, symbol, start, end, timeframe, df) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DepthCache + DepthCollector + SlippageModel + FeeModel + ValidationResult + 테스트</name>
  <files>engine/data/depth_cache.py, engine/data/depth_collector.py, engine/backtest/slippage.py, engine/backtest/fee_model.py, engine/backtest/validation_result.py, config/exchange_fees.json, tests/test_slippage.py</files>
  <behavior>
    - DepthCache.get_stats(symbol) returns dict with avg_spread_pct, avg_depth_usd_10 keys or None
    - DepthCache reads aggregated stats from Parquet files in cache_dir
    - DepthCache.save_snapshot(records: list[dict]) writes snapshot to Parquet
    - OrderbookDepthCollector.collect_snapshot(symbol) fetches orderbook via ccxt REST and returns stats dict
    - OrderbookDepthCollector.collect_top_symbols(n=50) fetches 24h volume Top N symbols
    - NoSlippage.calculate_slippage() always returns 0.0
    - VolumeAdjustedSlippage returns positive slippage proportional to order_size / depth
    - VolumeAdjustedSlippage falls back to 0.001 when depth cache has no data for symbol
    - FeeModel loads maker/taker rates from exchange_fees.json
    - FeeModel.get_fee_rate("binance", "futures", "taker") returns 0.0005
    - FeeModel.get_fee_rate for unknown exchange returns default 0.001
    - ValidationResult and WindowResult dataclasses are importable and construct correctly
  </behavior>
  <action>
    1. `engine/data/depth_cache.py`: DepthCache 클래스 구현.
       - __init__(cache_dir: Path = Path(".cache/depth"), ttl_days: int = 7)
       - get_stats(symbol: str) -> dict | None: cache_dir에서 해당 심볼의 Parquet 파일을 읽어 avg_spread_pct, avg_depth_usd_10 통계 반환. 파일 없거나 TTL 만료 시 None 반환.
       - save_snapshot(records: list[dict]) -> None: 스냅샷 데이터를 Parquet로 저장. 각 record는 symbol, timestamp, bid_depth_usd, ask_depth_usd, spread_pct 등 포함.
       - _aggregate_stats(df: pd.DataFrame) -> dict: 심볼별 평균 스프레드, 평균 depth 계산.
       - 기존 ParquetCache 패턴(ohlcv_cache.py) 참조하되, depth 전용으로 단순화.
       - RESEARCH.md Pitfall 5 참조: TTL 7일 설정, 만료 시 경고 로그.

    2. `engine/data/depth_collector.py`: OrderbookDepthCollector 클래스 구현.
       - __init__(exchange: str = "binance", cache_dir: Path = Path(".cache/depth"))
       - collect_snapshot(symbol: str, limit: int = 20) -> dict: ccxt fetch_order_book 호출, bid/ask depth USD, spread_pct, best_bid, best_ask 계산하여 dict 반환.
       - collect_top_symbols(n: int = 50) -> list[dict]: fetch_tickers()로 24h quoteVolume 기준 Top N 심볼 선택, 각각 collect_snapshot 호출.
       - save_to_cache(snapshots: list[dict]) -> None: DepthCache.save_snapshot() 호출하여 Parquet 저장.
       - RESEARCH.md 코드 예시 참조. ccxt REST 사용 (WebSocket 아님 -- RESEARCH.md 권장).

    3. `engine/backtest/slippage.py`: SlippageModel Protocol 정의 (calculate_slippage(symbol, side, order_size_usd, price) -> float). NoSlippage 구현 (항상 0.0 반환). VolumeAdjustedSlippage 구현 -- DepthCache 의존, slippage = base_spread + 0.1 * (order_size / depth_usd). depth 없으면 fallback 0.001. RESEARCH.md Pattern 1 참조.

    4. `engine/backtest/fee_model.py`: FeeModel 클래스 -- JSON 파일에서 거래소별 수수료 로드. get_fee_rate(exchange, market_type, side) -> float. 미등록 거래소 default 0.001. load_exchange_fees(path) -> dict 유틸리티.

    5. `config/exchange_fees.json`: binance (spot maker/taker 0.001, futures maker 0.0002/taker 0.0005), upbit (spot maker/taker 0.0005) 초기 설정.

    6. `engine/backtest/validation_result.py`: WindowResult(window_idx, is_sharpe, oos_sharpe, gap_ratio, passed) + ValidationResult(mode, windows, overall_passed, summary) dataclass 정의. RESEARCH.md Pattern 2 참조. 이 파일은 plan 02, 03에서 WalkForward/CPCV가 공유한다.

    7. `tests/test_slippage.py`:
       - DepthCache: get_stats 정상 반환 / None 반환 (파일 없음) 테스트
       - NoSlippage 반환값 0.0
       - VolumeAdjustedSlippage depth 비례 슬리피지 계산
       - VolumeAdjustedSlippage fallback 0.001 (depth 없음)
       - FeeModel JSON 로드, 미등록 거래소 default 테스트
       - NOTE: OrderbookDepthCollector는 ccxt 외부 의존이므로 unit test에서 mock 사용. 또는 통합 테스트로 별도 분리.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_slippage.py -x -v</automated>
  </verify>
  <done>DepthCache가 Parquet에서 depth 통계를 읽고, OrderbookDepthCollector가 ccxt REST로 수집하여 저장. SlippageModel 프로토콜로 NoSlippage/VolumeAdjustedSlippage 교체 가능, FeeModel이 JSON에서 수수료 로드, ValidationResult 인터페이스 정의 완료</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BacktestRunner 슬리피지+수수료 통합 + 테스트</name>
  <files>engine/backtest/runner.py, tests/test_backtest_costs.py</files>
  <behavior>
    - BacktestRunner(slippage_model=NoSlippage(), fee_rate=0.0) produces same result as current runner
    - BacktestRunner(slippage_model=VolumeAdjustedSlippage(...), fee_rate=0.001) produces lower final_capital than NoSlippage for same strategy
    - Entry price adjusted by slippage: entry_price = close * (1 + slippage_pct) for buy
    - Exit price adjusted by slippage: exit_price = close * (1 - slippage_pct) for sell
    - Fee deducted from capital on both entry and exit
    - BacktestRunner() with no arguments defaults to NoSlippage + fee_rate=0.0 (backward compatible)
  </behavior>
  <action>
    1. `engine/backtest/runner.py` 수정:
       - `__init__(self, slippage_model: SlippageModel | None = None, fee_rate: float = 0.0)` -- 기본값 NoSlippage, 0.0 (하위 호환)
       - `_simulate()` 내 entry 시점: slippage_pct = self._slippage_model.calculate_slippage(symbol, "buy", capital, close); entry_price = close * (1 + slippage_pct); entry_fee = entry_price * self._fee_rate; capital -= entry_fee
       - exit 시점: slippage_pct 계산; exit_price = close * (1 - slippage_pct); exit_fee = exit_price * self._fee_rate; pnl 계산 후 fee 차감
       - symbol을 _simulate에 전달해야 함 -- run()에서 _simulate(df, initial_capital, exposure_series, symbol) 호출로 변경
       - RESEARCH.md Pattern 4 참조

    2. `tests/test_backtest_costs.py`:
       - 기본 생성자 하위 호환 테스트 (NoSlippage, fee=0)
       - VolumeAdjustedSlippage 적용 시 수익률 감소 테스트
       - fee_rate 적용 시 수익률 감소 테스트
       - 슬리피지+수수료 동시 적용 테스트
       - 테스트용 간단한 OHLCV DataFrame + signal 컬럼 사용 (deterministic)
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_backtest_costs.py -x -v</automated>
  </verify>
  <done>BacktestRunner가 슬리피지+수수료를 entry/exit 가격에 적용하고, NoSlippage 기본값으로 기존 동작과 동일, VolumeAdjustedSlippage 적용 시 수익률이 낮아짐을 테스트로 증명</done>
</task>

</tasks>

<verification>
```bash
.venv/bin/python -m pytest tests/test_slippage.py tests/test_backtest_costs.py -x -v
```
전체 기존 테스트 회귀 확인:
```bash
.venv/bin/python -m pytest tests/ -x -q
```
</verification>

<success_criteria>
1. DepthCache가 Parquet에서 심볼별 depth 통계(avg_spread_pct, avg_depth_usd_10)를 읽어 반환
2. OrderbookDepthCollector가 ccxt REST fetch_order_book으로 스냅샷 수집 + Parquet 저장
3. NoSlippage 기본값으로 기존 BacktestRunner 동작과 동일 (하위 호환)
4. VolumeAdjustedSlippage 적용 시 동일 전략 수익률이 감소
5. exchange_fees.json에서 거래소별 수수료 로드
6. ValidationResult 인터페이스가 정의되어 후속 plan에서 import 가능
7. 기존 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-backtest-quality-gates/02-01-SUMMARY.md`
</output>
