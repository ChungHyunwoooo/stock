---
phase: 02-backtest-quality-gates
plan: 06
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - engine/backtest/report.py
  - tests/test_backtest_report.py
autonomous: true
requirements: [BT-02, BT-05]

must_haves:
  truths:
    - "quantstats HTML tearsheet가 equity curve에서 생성된다"
    - "IS/OOS 분할 시각화 차트가 walk-forward ValidationResult에서 생성된다"
    - "equity curve 시각화가 matplotlib로 생성된다"
    - "최종 통합 판정(WF/CPCV 결과 + 메트릭 테이블)이 포함된 리포트가 생성된다"
  artifacts:
    - path: "engine/backtest/report.py"
      provides: "quantstats tearsheet + IS/OOS chart + equity curve + 통합 판정 리포트"
      exports: ["generate_quantstats_report", "generate_validation_chart", "generate_full_report"]
    - path: "tests/test_backtest_report.py"
      provides: "리포트 생성 테스트"
      min_lines: 40
  key_links:
    - from: "engine/backtest/report.py"
      to: "engine/backtest/validation_result.py"
      via: "import ValidationResult for chart generation"
      pattern: "from engine\\.backtest\\.validation_result import"
    - from: "engine/backtest/report.py"
      to: "quantstats"
      via: "qs.reports.html() for tearsheet generation"
      pattern: "quantstats|qs\\.reports"
---

<objective>
quantstats 연동 + IS/OOS 분할 시각화 + equity curve 차트 + 최종 통합 판정 리포트 생성

Purpose: CONTEXT.md locked "결과 출력: quantstats 연동, equity curve 시각화, IS/OOS 분할 시각화, 최종 통합 판정 포함" 요구사항 충족. 백테스트 검증 결과를 시각적으로 확인 가능하게 한다.
Output: 확장된 report.py, test_backtest_report.py
</objective>

<execution_context>
@/home/hwchung/.claude/get-shit-done/workflows/execute-plan.md
@/home/hwchung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backtest-quality-gates/02-CONTEXT.md
@.planning/phases/02-backtest-quality-gates/02-RESEARCH.md
@.planning/phases/02-backtest-quality-gates/02-02-SUMMARY.md
@.planning/phases/02-backtest-quality-gates/02-03-SUMMARY.md

<interfaces>
From engine/backtest/report.py (기존 -- 확장 대상):
```python
def generate_summary(result: BacktestResult) -> dict: ...
def generate_report(result: BacktestResult, output_path: str | None = None) -> str: ...
# 기존 함수 유지하면서 아래 함수들을 추가
```

From engine/backtest/validation_result.py (created by plan 01):
```python
@dataclass(slots=True)
class WindowResult:
    window_idx: int
    is_sharpe: float
    oos_sharpe: float
    gap_ratio: float
    passed: bool

@dataclass(slots=True)
class ValidationResult:
    mode: str              # "walk_forward" | "cpcv"
    windows: list[WindowResult]
    overall_passed: bool
    summary: dict
```

From engine/backtest/runner.py:
```python
@dataclass
class BacktestResult:
    symbol: str
    timeframe: str
    equity_curve: pd.Series
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: quantstats tearsheet + IS/OOS 시각화 + 통합 리포트 구현</name>
  <files>engine/backtest/report.py, tests/test_backtest_report.py</files>
  <behavior>
    - generate_quantstats_report(equity_curve, title, output) returns HTML file path
    - generate_quantstats_report creates valid HTML file using quantstats.reports.html()
    - generate_validation_chart(validation_result, output) returns PNG file path
    - generate_validation_chart creates IS/OOS Sharpe bar chart with PASS/FAIL color coding per window
    - generate_full_report(backtest_result, validation_result, output) returns HTML file path
    - generate_full_report includes: equity curve chart + quantstats metrics + IS/OOS split chart + overall judgment
    - All functions create output directory if not exists
    - Edge case: empty equity curve returns path but with "No data" message
  </behavior>
  <action>
    1. `engine/backtest/report.py` 확장 -- 기존 generate_summary, generate_report 함수는 그대로 유지하고 아래 추가:

       - generate_quantstats_report(equity_curve: pd.Series, title: str = "Strategy Tearsheet", output: str | None = None, benchmark: str | None = None) -> str:
         - returns = equity_curve.pct_change().dropna()
         - output_path 기본값: .cache/reports/{title}.html
         - quantstats.reports.html(returns, benchmark=benchmark, title=title, output=output_path, periods_per_year=252)
         - RESEARCH.md quantstats 코드 예시 참조

       - generate_validation_chart(validation_result: ValidationResult, output: str | None = None) -> str:
         - matplotlib bar chart: x축 = window_idx, y축 = Sharpe (IS green, OOS blue)
         - 각 윈도우 PASS/FAIL 색상 표시 (PASS=green border, FAIL=red border)
         - gap_threshold 수평선 표시
         - 제목에 mode("walk_forward" | "cpcv") + overall_passed 표시
         - output_path 기본값: .cache/reports/validation_{mode}.png

       - generate_full_report(backtest_result: BacktestResult, validation_result: ValidationResult | None = None, output: str | None = None) -> str:
         - 기존 generate_report() 기반으로 확장
         - 추가 섹션: IS/OOS 분할 시각화 (validation_result가 있을 때), 통합 판정 (PASS/FAIL + 근거 테이블)
         - quantstats 통계 요약 테이블 추가 (returns, volatility, Sharpe, max_drawdown, Calmar 등)
         - HTML 내 matplotlib 차트는 base64 인라인 이미지로 삽입

    2. `tests/test_backtest_report.py`:
       - generate_quantstats_report: 합성 equity curve로 HTML 파일 생성 확인
       - generate_validation_chart: 합성 ValidationResult로 PNG 파일 생성 확인
       - generate_full_report: backtest_result + validation_result로 HTML 파일 생성 확인
       - generate_full_report: validation_result=None 시에도 정상 동작
       - 출력 파일이 비어 있지 않은지 확인 (size > 0)
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_backtest_report.py -x -v</automated>
  </verify>
  <done>quantstats HTML tearsheet 생성, IS/OOS 분할 차트 생성, 통합 판정 리포트 생성. CONTEXT.md의 "결과 출력: quantstats 연동, equity curve 시각화, IS/OOS 분할 시각화, 최종 통합 판정 포함" 충족.</done>
</task>

</tasks>

<verification>
```bash
.venv/bin/python -m pytest tests/test_backtest_report.py -x -v
```
기존 테스트 회귀:
```bash
.venv/bin/python -m pytest tests/ -x -q
```
</verification>

<success_criteria>
1. quantstats HTML tearsheet가 equity curve에서 정상 생성
2. IS/OOS 분할 시각화 차트가 ValidationResult에서 PNG로 생성
3. 통합 리포트가 equity curve + 메트릭 + IS/OOS 차트 + 판정을 포함
4. 기존 generate_summary, generate_report 함수 하위 호환 유지
5. 기존 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-backtest-quality-gates/02-06-SUMMARY.md`
</output>
