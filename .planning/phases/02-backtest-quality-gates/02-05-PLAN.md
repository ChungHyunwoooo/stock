---
phase: 02-backtest-quality-gates
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - engine/core/db_models.py
  - engine/core/repository.py
  - engine/core/database.py
  - engine/backtest/runner.py
  - tests/test_backtest_history.py
autonomous: true
requirements: [BT-04]

must_haves:
  truths:
    - "BacktestRunner.run() 완료 시 결과가 자동으로 DB에 저장된다"
    - "저장 실패는 warning 로그만 남기고 백테스트 결과는 정상 반환된다"
    - "전략별 시간순 이력 조회가 가능하다"
    - "전략 간 횡단 비교 조회가 가능하다"
    - "BacktestRecord에 slippage_model, fee_rate, wf_result, cpcv_mode, multi_symbol_result 칼럼이 추가된다"
  artifacts:
    - path: "engine/core/db_models.py"
      provides: "BacktestRecord 확장 칼럼"
      contains: "slippage_model"
    - path: "engine/core/repository.py"
      provides: "BacktestRepository 이력 조회 + 비교 메서드"
      exports: ["BacktestRepository"]
    - path: "engine/core/database.py"
      provides: "Phase 2 스키마 마이그레이션"
      contains: "_migrate_backtests_phase2"
    - path: "tests/test_backtest_history.py"
      provides: "DB 저장 + 이력 조회 테스트"
      min_lines: 60
  key_links:
    - from: "engine/backtest/runner.py"
      to: "engine/core/repository.py"
      via: "run() 완료 후 BacktestRepository.save() 자동 호출"
      pattern: "BacktestRepository\\(\\)\\.save"
    - from: "engine/core/database.py"
      to: "engine/core/db_models.py"
      via: "init_db() 내 _migrate_backtests_phase2() 호출"
      pattern: "_migrate_backtests_phase2"
---

<objective>
백테스트 결과 DB 자동 저장 + BacktestRecord 스키마 확장 + 이력 비교 조회 구현

Purpose: 백테스트 결과를 체계적으로 저장하여 전략별/시간별 성과 추이를 추적하고 비교할 수 있게 한다.
Output: 수정된 db_models.py, repository.py, database.py, runner.py, test_backtest_history.py
</objective>

<execution_context>
@/home/hwchung/.claude/get-shit-done/workflows/execute-plan.md
@/home/hwchung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backtest-quality-gates/02-CONTEXT.md
@.planning/phases/02-backtest-quality-gates/02-RESEARCH.md
@.planning/phases/02-backtest-quality-gates/02-01-SUMMARY.md

<interfaces>
From engine/core/db_models.py (현재):
```python
class BacktestRecord(Base):
    __tablename__ = "backtests"
    id: Mapped[int] = mapped_column(primary_key=True)
    strategy_id: Mapped[int] = mapped_column(ForeignKey("strategies.id"))
    symbol: Mapped[str] = mapped_column(String(50))
    timeframe: Mapped[str] = mapped_column(String(10))
    start_date: Mapped[str] = mapped_column(String(10))
    end_date: Mapped[str] = mapped_column(String(10))
    total_return: Mapped[float] = mapped_column(Float)
    sharpe_ratio: Mapped[float | None] = mapped_column(Float, nullable=True)
    max_drawdown: Mapped[float | None] = mapped_column(Float, nullable=True)
    result_json: Mapped[str] = mapped_column(Text)
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
```

From engine/core/repository.py (현재):
```python
class BacktestRepository:
    def save(self, session, record) -> BacktestRecord: ...
    def get(self, session, backtest_id) -> BacktestRecord | None: ...
    def get_by_strategy(self, session, strategy_id) -> list[BacktestRecord]: ...
    def list_all(self, session) -> list[BacktestRecord]: ...
```

From engine/core/database.py (현재):
```python
def get_engine(db_url="sqlite:///tse.db") -> Engine: ...
def get_session() -> Generator[Session, None, None]: ...
def init_db(db_url="sqlite:///tse.db") -> None: ...
```

From engine/backtest/runner.py (plan 01 수정 후):
```python
class BacktestRunner:
    def __init__(self, slippage_model=None, fee_rate=0.0): ...
    def run(self, strategy, symbol, start, end, ...) -> BacktestResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BacktestRecord 스키마 확장 + 마이그레이션 + Repository 이력 조회</name>
  <files>engine/core/db_models.py, engine/core/repository.py, engine/core/database.py, tests/test_backtest_history.py</files>
  <behavior>
    - BacktestRecord has new columns: slippage_model(str), fee_rate(float), wf_result(str|None), cpcv_mode(bool), multi_symbol_result(str|None)
    - _migrate_backtests_phase2 adds missing columns to existing SQLite DB without data loss
    - init_db() calls _migrate_backtests_phase2() after create_all()
    - BacktestRepository.get_history(session, strategy_id, order_by="created_at") returns time-ordered list
    - BacktestRepository.compare_strategies(session, strategy_ids) returns cross-strategy comparison
    - BacktestRepository.delete(session, backtest_id) deletes a record
    - BacktestRepository.delete_by_strategy(session, strategy_id) deletes all records for a strategy
  </behavior>
  <action>
    1. `engine/core/db_models.py` 수정 — BacktestRecord에 칼럼 추가:
       - slippage_model: Mapped[str] = mapped_column(String(50), default="none")
       - fee_rate: Mapped[float] = mapped_column(Float, default=0.0)
       - wf_result: Mapped[str | None] = mapped_column(String(10), nullable=True)  # PASS/FAIL/None
       - cpcv_mode: Mapped[bool] = mapped_column(Boolean, default=False)
       - multi_symbol_result: Mapped[str | None] = mapped_column(Text, nullable=True)  # JSON
       - CONTEXT.md locked: 정확히 이 칼럼들

    2. `engine/core/database.py` 수정 — _migrate_backtests_phase2(engine) 함수 추가:
       - PRAGMA table_info(backtests)로 기존 칼럼 확인
       - 누락 칼럼만 ALTER TABLE ADD COLUMN
       - init_db() 내 create_all() 후 _migrate_backtests_phase2(engine) 호출
       - RESEARCH.md Pitfall 4 + SQLite migration 코드 참조

    3. `engine/core/repository.py` 수정 — BacktestRepository 메서드 추가:
       - get_history(session, strategy_id, limit=100) -> list[BacktestRecord]: created_at DESC 정렬
       - compare_strategies(session, strategy_ids: list[int]) -> list[BacktestRecord]: 여러 전략의 최근 결과 조회
       - delete(session, backtest_id: int) -> None
       - delete_by_strategy(session, strategy_id: int) -> None
       - CONTEXT.md: "이력 비교: 전략 내 시간순 이력 + 전략 간 횡단 비교"

    4. `tests/test_backtest_history.py`:
       - in-memory SQLite로 BacktestRecord 저장/조회 (신규 칼럼 포함)
       - get_history 시간순 정렬 테스트
       - compare_strategies 다전략 횡단 비교 테스트
       - delete + delete_by_strategy 테스트
       - _migrate_backtests_phase2: 기존 테이블에 칼럼 추가 테스트
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_backtest_history.py -x -v</automated>
  </verify>
  <done>BacktestRecord에 Phase 2 칼럼 추가, 기존 DB 자동 마이그레이션, 이력 조회 + 횡단 비교 + 삭제 기능 동작</done>
</task>

<task type="auto">
  <name>Task 2: BacktestRunner 자동 DB 저장 통합</name>
  <files>engine/backtest/runner.py, tests/test_backtest_history.py</files>
  <action>
    1. `engine/backtest/runner.py` 수정 — run() 메서드 끝에 자동 저장 로직 추가:
       - __init__에 auto_save: bool = True, strategy_id: int | None = None 파라미터 추가
       - run() 완료 후 auto_save=True이고 strategy_id가 있으면:
         - try: get_session()으로 BacktestRecord 생성 + save
         - except: logger.warning("백테스트 결과 DB 저장 실패: %s", e) — 백테스트 결과는 정상 반환
       - BacktestRecord 생성 시:
         - strategy_id, symbol, timeframe, start_date, end_date, total_return, sharpe_ratio, max_drawdown
         - result_json = BacktestResult.to_result_json()
         - slippage_model = type(self._slippage_model).__name__ (e.g. "NoSlippage", "VolumeAdjustedSlippage")
         - fee_rate = self._fee_rate
       - CONTEXT.md locked: "자동 저장: BacktestRunner.run() 완료 시 항상 자동 DB 저장 (저장 실패는 warning)"

    2. `tests/test_backtest_history.py`에 통합 테스트 추가:
       - auto_save=True + strategy_id 설정 시 DB에 레코드 생성 확인
       - auto_save=False 시 DB에 레코드 미생성 확인
       - DB 연결 실패 시에도 BacktestResult 정상 반환 확인 (mock get_session raise)
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_backtest_history.py -x -v</automated>
  </verify>
  <done>BacktestRunner.run() 완료 시 자동 DB 저장, 저장 실패는 warning만 남기고 결과 정상 반환</done>
</task>

</tasks>

<verification>
```bash
.venv/bin/python -m pytest tests/test_backtest_history.py -x -v
```
전체 회귀:
```bash
.venv/bin/python -m pytest tests/ -x -q
```
</verification>

<success_criteria>
1. BacktestRecord에 slippage_model, fee_rate, wf_result, cpcv_mode, multi_symbol_result 칼럼 존재
2. 기존 DB에 _migrate_backtests_phase2가 칼럼 자동 추가 (데이터 손실 없음)
3. BacktestRunner.run() 완료 시 자동 DB 저장 (auto_save=True 기본)
4. 저장 실패는 warning 로그만 — 백테스트 결과 정상 반환
5. 전략별 시간순 이력 + 전략 간 횡단 비교 조회 가능
6. 기존 테스트 전체 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-backtest-quality-gates/02-05-SUMMARY.md`
</output>
