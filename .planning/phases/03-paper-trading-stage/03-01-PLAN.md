---
phase: 03-paper-trading-stage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/core/db_models.py
  - engine/core/database.py
  - engine/core/repository.py
  - engine/execution/paper_broker.py
  - config/paper_trading.json
  - tests/test_paper_broker_persistence.py
autonomous: true
requirements: [LIFE-02]
must_haves:
  truths:
    - "PaperBroker 상태(잔고, 포지션)가 프로세스 재시작 후에도 보존된다"
    - "거래 발생 시마다 누적 PnL이 기록되고, 일별 스냅샷도 별도 기록된다"
    - "재시작 시 잔고/포지션만 복원되고 미체결 주문은 자동 취소된다"
    - "전략별로 paper 세션이 격리되어 서로 간섭하지 않는다"
  artifacts:
    - path: "engine/core/db_models.py"
      provides: "PaperBalance, PaperPnlSnapshot SQLAlchemy 모델"
      contains: "class PaperBalance"
    - path: "engine/core/repository.py"
      provides: "PaperRepository CRUD"
      contains: "class PaperRepository"
    - path: "engine/execution/paper_broker.py"
      provides: "DB 영속화 PaperBroker"
      contains: "save_balance"
    - path: "config/paper_trading.json"
      provides: "승격 기준 기본값 + 체크 주기 설정"
  key_links:
    - from: "engine/execution/paper_broker.py"
      to: "engine/core/repository.py"
      via: "PaperRepository 호출"
      pattern: "PaperRepository"
    - from: "engine/execution/paper_broker.py"
      to: "engine/core/database.py"
      via: "get_session() 컨텍스트 매니저"
      pattern: "get_session"
    - from: "engine/core/database.py"
      to: "engine/core/db_models.py"
      via: "migration에서 paper 테이블 생성"
      pattern: "_migrate_paper_phase3"
---

<objective>
PaperBroker를 SQLite DB 영속화하여 프로세스 재시작 후에도 잔고/포지션/PnL이 보존되도록 한다.

Purpose: 페이퍼 트레이딩 중인 전략의 상태가 세션 간 유지되어야 승격 판정에 필요한 연속적인 성과 데이터를 축적할 수 있다.
Output: PaperBalance/PaperPnlSnapshot 모델, PaperRepository, DB 영속화된 PaperBroker, paper_trading.json 설정 파일
</objective>

<execution_context>
@/home/hwchung/.claude/get-shit-done/workflows/execute-plan.md
@/home/hwchung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-paper-trading-stage/03-CONTEXT.md
@.planning/phases/03-paper-trading-stage/03-RESEARCH.md

@engine/core/db_models.py
@engine/core/database.py
@engine/core/repository.py
@engine/execution/paper_broker.py
@engine/execution/broker_base.py
@engine/schema.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From engine/core/db_models.py:
```python
class Base(DeclarativeBase): pass
class TradeRecord(Base):
    __tablename__ = "trades"
    broker: Mapped[str] = mapped_column(String(10), default="paper")  # paper / live
    strategy_name: Mapped[str] = mapped_column(String(200), index=True)
    symbol: Mapped[str] = mapped_column(String(50), index=True)
    profit_abs: Mapped[float | None] = mapped_column(Float, nullable=True)
    profit_pct: Mapped[float | None] = mapped_column(Float, nullable=True)
    status: Mapped[str] = mapped_column(String(20), default="open", index=True)
```

From engine/core/database.py:
```python
def get_engine(db_url: str = "sqlite:///tse.db") -> Engine
def get_session() -> Generator[Session, None, None]  # contextmanager
def init_db(db_url: str = "sqlite:///tse.db") -> None  # calls create_all + migrations
def _migrate_backtests_phase2(engine: Engine) -> None  # idempotent PRAGMA pattern
```

From engine/core/repository.py:
```python
class BacktestRepository:
    def save(self, session, record) -> BacktestRecord
    def get_history(self, session, strategy_id, limit=100) -> list[BacktestRecord]
class TradeRepository:
    def list_closed(self, session, symbol=None, strategy_name=None, broker=None, limit=100) -> list[TradeRecord]
    def summary(self, session, strategy_name=None, broker=None) -> dict
```

From engine/execution/paper_broker.py:
```python
class PaperBroker(BaseBroker):
    exchange_name = "paper"
    broker_kind = BrokerKind.paper
    def __init__(self, initial_balance: float = 10_000_000) -> None
    def _place_order(self, order, converted_symbol) -> ExecutionRecord
    def _fetch_raw_balance(self) -> dict[str, Any]
    def _fetch_raw_positions(self) -> list[dict[str, Any]]
```

From engine/execution/broker_base.py:
```python
class BaseBroker(ABC):
    # _place_order, _fetch_raw_balance, _fetch_raw_positions, _cancel_raw, _convert_symbol
```

From engine/schema.py:
```python
class StrategyStatus(str, Enum): draft, testing, paper, active, archived
class StrategyDefinition(BaseModel):
    timeframes: list[str] = Field(default_factory=lambda: ["1d"])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB 모델 + Migration + PaperRepository + Config</name>
  <files>engine/core/db_models.py, engine/core/database.py, engine/core/repository.py, config/paper_trading.json, tests/test_paper_broker_persistence.py</files>
  <behavior>
    - PaperBalance 모델: strategy_id, balance, equity, unrealized_pnl, snapshot_at 칼럼. save/load round-trip 성공
    - PaperPnlSnapshot 모델: strategy_id, date, cumulative_pnl, daily_pnl, trade_count, win_count, equity. UNIQUE(strategy_id, date) 제약
    - PaperPnlSnapshot upsert: 같은 (strategy_id, date)에 두 번 저장하면 UPDATE (INSERT OR REPLACE)
    - _migrate_paper_phase3(): 이미 테이블 존재 시 재실행해도 에러 없음 (idempotent)
    - PaperRepository.save_balance(): PaperBalance 저장 후 id 반환
    - PaperRepository.get_latest_balance(): strategy_id로 가장 최신 잔고 조회
    - PaperRepository.save_daily_snapshot(): PnL 스냅샷 upsert
    - PaperRepository.get_daily_snapshots(): strategy_id로 일별 스냅샷 조회 (newest first, limit)
    - PaperRepository.get_paper_strategies(): paper 거래가 있는 전략 목록 반환
    - config/paper_trading.json: promotion_gates 기본값 + check_interval_hours + 타임프레임별 min_trades 매핑
  </behavior>
  <action>
1. engine/core/db_models.py에 PaperBalance, PaperPnlSnapshot SQLAlchemy 모델 추가:
   - PaperBalance: id(PK), strategy_id(VARCHAR 100, index), balance(REAL), equity(REAL), unrealized_pnl(REAL, default 0), snapshot_at(DATETIME), created_at(DATETIME, server_default)
   - PaperPnlSnapshot: id(PK), strategy_id(VARCHAR 100), date(VARCHAR 10), cumulative_pnl(REAL), daily_pnl(REAL), trade_count(INTEGER, default 0), win_count(INTEGER, default 0), equity(REAL), created_at(DATETIME, server_default). UniqueConstraint(strategy_id, date)

2. engine/core/database.py에 _migrate_paper_phase3(engine) 함수 추가:
   - CREATE TABLE IF NOT EXISTS 패턴 (Phase 2 _migrate_backtests_phase2 참조)
   - paper_balances + paper_pnl_snapshots 테이블 생성
   - init_db()에서 _migrate_paper_phase3(engine) 호출 추가

3. engine/core/repository.py에 PaperRepository 클래스 추가:
   - save_balance(session, record) -> PaperBalance
   - get_latest_balance(session, strategy_id) -> PaperBalance | None: ORDER BY snapshot_at DESC LIMIT 1
   - save_daily_snapshot(session, record) -> PaperPnlSnapshot: INSERT OR REPLACE on (strategy_id, date)
   - get_daily_snapshots(session, strategy_id, limit=90) -> list[PaperPnlSnapshot]: ORDER BY date DESC
   - get_paper_strategies(session) -> list[str]: SELECT DISTINCT strategy_id FROM paper_pnl_snapshots

4. config/paper_trading.json 생성:
   ```json
   {
     "check_interval_hours": 6,
     "promotion_gates": {
       "min_days": 7,
       "min_sharpe": 0.3,
       "min_win_rate": 0.30,
       "max_drawdown": -0.20,
       "min_cumulative_pnl": 0.0
     },
     "timeframe_min_trades": {
       "1m": 20, "5m": 20, "15m": 20,
       "1h": 10, "4h": 10,
       "1d": 5, "1w": 5
     }
   }
   ```
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_paper_broker_persistence.py -x -q</automated>
  </verify>
  <done>PaperBalance/PaperPnlSnapshot 모델이 DB에 생성되고, PaperRepository의 save/get round-trip이 테스트 통과하며, config 파일이 존재한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PaperBroker DB 영속화 (저장/복원/PnL 이중 기록)</name>
  <files>engine/execution/paper_broker.py, tests/test_paper_broker_persistence.py</files>
  <behavior>
    - PaperBroker(strategy_id="test", initial_balance=10000): strategy_id 필수 파라미터
    - PaperBroker 초기화 시 DB에 이전 잔고가 있으면 복원, 없으면 initial_balance 사용
    - _place_order() 호출 후 DB에 잔고 스냅샷 자동 저장 (try/except로 DB 실패 시 거래는 계속)
    - _place_order() 호출 시 누적 PnL 기록 (거래별 PnL 스냅샷)
    - save_daily_snapshot() 호출 시 당일 집계를 PaperPnlSnapshot에 upsert
    - 재시작 시: 잔고/포지션 복원, 미체결 주문 없음 (Paper 특성상 즉시 체결)
    - DB 세션은 각 작업마다 get_session() 컨텍스트 매니저로 열고 닫음 (장시간 세션 보유 금지)
  </behavior>
  <action>
1. PaperBroker.__init__() 시그니처 변경:
   - `def __init__(self, strategy_id: str, initial_balance: float = 10_000_000, db_url: str | None = None) -> None`
   - strategy_id 필수. db_url이 None이면 기본 DB 사용
   - `with get_session() as session:` 패턴으로 DB에서 최신 잔고 조회
   - 이전 잔고가 있으면 balance/equity 복원, 없으면 initial_balance 사용
   - 포지션 복원: TradeRepository.list_open(session, strategy_name=strategy_id) 에서 broker="paper" 필터

2. _place_order() 수정:
   - 기존 ExecutionRecord 반환 로직 유지
   - 체결 후 try/except 블록에서:
     - PaperRepository.save_balance()로 현재 잔고 스냅샷 저장
     - 잔고 업데이트 (매수 시 차감, 매도 시 가산)
   - DB 실패 시 logger.warning() 후 정상 반환 (Phase 2 패턴: DB failure never blocks)

3. save_daily_snapshot() 메서드 추가:
   - `with get_session() as session:` 패턴
   - TradeRepository.list_closed()로 당일 paper 거래 집계
   - 누적 PnL = 전체 closed 거래의 profit_abs 합산
   - 일별 PnL = 당일 closed 거래의 profit_abs 합산
   - PaperRepository.save_daily_snapshot()으로 upsert

4. _fetch_raw_balance() 수정:
   - DB에서 최신 잔고 반환 (in-memory 캐시 + DB 동기화)

5. _fetch_raw_positions() 수정:
   - TradeRepository.list_open()으로 열린 paper 포지션 반환
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_paper_broker_persistence.py -x -q</automated>
  </verify>
  <done>PaperBroker가 strategy_id 기반으로 DB에 잔고를 저장/복원하고, 거래 발생 시 PnL이 이중 기록되며, 재시작 시 상태가 보존된다</done>
</task>

</tasks>

<verification>
```bash
# 전체 테스트 통과 확인
.venv/bin/python -m pytest tests/test_paper_broker_persistence.py -x -v

# 기존 테스트 회귀 확인
.venv/bin/python -m pytest --timeout=60 -x -q
```
</verification>

<success_criteria>
- PaperBroker(strategy_id="test")가 DB에 잔고를 저장하고, 새 인스턴스가 같은 strategy_id로 복원할 수 있다
- 거래 후 paper_balances에 스냅샷이 기록된다
- save_daily_snapshot() 호출 시 paper_pnl_snapshots에 당일 집계가 upsert된다
- 기존 전체 테스트가 깨지지 않는다
</success_criteria>

<output>
After completion, create `.planning/phases/03-paper-trading-stage/03-01-SUMMARY.md`
</output>
