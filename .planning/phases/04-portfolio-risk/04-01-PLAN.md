---
phase: 04-portfolio-risk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/strategy/scalping_risk.py
  - engine/strategy/risk_parity.py
  - engine/strategy/position_sizer.py
  - tests/test_position_sizer.py
  - tests/test_risk_parity.py
autonomous: true
requirements: [RISK-02]

must_haves:
  truths:
    - "ATR+Kelly 기반 포지션 사이징이 고정 2% 방식과 다른 값을 반환한다"
    - "거래 이력 20건 미만이면 고정 risk_per_trade_pct를 사용하고, 20건 이상이면 Quarter Kelly를 적용한다"
    - "Risk Parity가 전략별 리스크 기여를 동등하게 배분한다"
    - "최종 포지션 크기 = ATR+Kelly 수량 x RiskManager.position_size_factor() x Risk Parity 배분 비율"
  artifacts:
    - path: "engine/strategy/position_sizer.py"
      provides: "통합 포지션 사이징 오케스트레이터 (ATR+Kelly + position_size_factor 곱산)"
      exports: ["PositionSizer", "PositionSizeResult"]
    - path: "engine/strategy/risk_parity.py"
      provides: "Risk Parity 자본 배분 (numpy 직접 구현)"
      exports: ["calculate_risk_parity_weights", "RiskParityConfig"]
    - path: "engine/strategy/scalping_risk.py"
      provides: "타임프레임별 프리셋이 추가된 ScalpRiskConfig"
      contains: "TIMEFRAME_PRESETS"
    - path: "tests/test_position_sizer.py"
      provides: "PositionSizer 단위 테스트"
    - path: "tests/test_risk_parity.py"
      provides: "Risk Parity 단위 테스트"
  key_links:
    - from: "engine/strategy/position_sizer.py"
      to: "engine/strategy/scalping_risk.py"
      via: "calculate_scalp_risk() 호출"
      pattern: "calculate_scalp_risk"
    - from: "engine/strategy/position_sizer.py"
      to: "engine/strategy/risk_manager.py"
      via: "position_size_factor() 곱산"
      pattern: "position_size_factor"
    - from: "engine/strategy/position_sizer.py"
      to: "engine/strategy/risk_parity.py"
      via: "risk_parity_weights 배분 비율 적용"
      pattern: "calculate_risk_parity_weights"
---

<objective>
ATR+Kelly 기반 변동성 포지션 사이징 + Risk Parity 자본 배분을 구현하여 고정 2% 사이징을 완전 대체한다.

Purpose: 시장 변동성과 전략 성과에 따라 포지션 크기가 동적으로 조절되어 리스크 대비 수익을 극대화하고, 다전략 운용 시 리스크 기여가 균등하게 배분된다.
Output: PositionSizer (통합 사이징), risk_parity.py (자본 배분), scalping_risk.py 타임프레임 프리셋
</objective>

<execution_context>
@/home/hwchung/.claude/get-shit-done/workflows/execute-plan.md
@/home/hwchung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-portfolio-risk/04-CONTEXT.md

<interfaces>
<!-- 기존 코드 계약 — executor가 직접 사용할 인터페이스 -->

From engine/strategy/scalping_risk.py:
```python
@dataclass(slots=True)
class ScalpRiskConfig:
    atr_period: int = 14
    atr_lookback: int = 100
    sl_mult_min: float = 0.8
    sl_mult_max: float = 2.5
    rr_min: float = 1.5
    rr_max: float = 3.0
    leverage_min: int = 2
    leverage_max: int = 20
    risk_per_trade_pct: float = 0.02
    max_position_pct: float = 0.1
    max_loss_pct: float = 0.5
    min_sl_pct: float = 0.1
    max_sl_pct: float = 2.0

@dataclass(slots=True)
class ScalpRiskResult:
    stop_loss: float
    take_profit: float
    leverage: int
    quantity: float
    sl_pct: float
    tp_pct: float
    rr_ratio: float
    atr: float
    atr_pct: float
    atr_pctile: float
    risk_amount: float
    position_value: float
    reason: str

def fractional_kelly(win_rate, avg_win, avg_loss, fraction=0.25) -> float: ...
def calculate_scalp_risk(df, entry_price, side, capital, config=None, kelly_fraction=None, trade_stats=None) -> ScalpRiskResult: ...
```

From engine/strategy/risk_manager.py:
```python
class RiskConfig:
    max_positions_per_symbol: int = 1
    max_total_positions: int = 5
    max_daily_loss_pct: float = 5.0
    max_consecutive_sl: int = 3
    cooldown_bars_after_sl: int = 5
    drawdown_reduce_threshold: float = 2.0
    drawdown_reduce_factor: float = 0.5

class RiskManager:
    def allow_entry(self, symbol: str, signal: PatternSignal) -> bool: ...
    def position_size_factor(self) -> float:  # 0~1, 드로다운 기반 축소
    def on_entry(self, symbol: str) -> None: ...
    def on_exit(self, symbol: str, pnl_pct: float, exit_reason: str, bar_index: int = 0) -> None: ...
```

From engine/strategy/risk.py:
```python
def calculate_position_size(capital, risk_params, entry_price, stop_price) -> float: ...
```

From engine/schema.py:
```python
class RiskParams(BaseModel):
    stop_loss_pct: float | None = None
    take_profit_pct: float | None = None
    risk_per_trade_pct: float = 0.02
    trailing_stop_pct: float | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scalping_risk.py 타임프레임 프리셋 + PositionSizer 통합 모듈</name>
  <files>engine/strategy/scalping_risk.py, engine/strategy/position_sizer.py, tests/test_position_sizer.py</files>
  <behavior>
    - Test 1: ScalpRiskConfig.for_timeframe("5m") 호출 시 스캘핑 프리셋 반환 (sl_mult_min/max, rr_min/max 등이 기본값과 다름)
    - Test 2: ScalpRiskConfig.for_timeframe("1d") 호출 시 스윙 프리셋 반환
    - Test 3: ScalpRiskConfig.for_timeframe("unknown") 호출 시 기본값(데이트레이딩) 반환
    - Test 4: PositionSizer.calculate()가 ATR+Kelly 수량 x position_size_factor를 반환 (kelly_fraction=0.25, 거래 이력 20건 이상)
    - Test 5: PositionSizer.calculate()에 거래 이력 20건 미만 전달 시 고정 risk_per_trade_pct 사용 (Kelly 미적용)
    - Test 6: PositionSizer.calculate()에 position_size_factor=0.5 전달 시 결과 quantity가 factor=1.0 대비 절반
    - Test 7: PositionSizer.calculate()에 allocation_weight=0.3 전달 시 capital * 0.3으로 사이징
  </behavior>
  <action>
1. scalping_risk.py에 TIMEFRAME_PRESETS dict 추가 (classmethod ScalpRiskConfig.for_timeframe(tf: str)):
   - "1m"/"5m" (스캘핑): sl_mult_min=0.5, sl_mult_max=1.5, rr_min=1.2, rr_max=2.5, min_sl_pct=0.05, max_sl_pct=0.5
   - "15m"/"30m" (데이트레이딩): 기본값 유지 (현재 ScalpRiskConfig 디폴트)
   - "1h"/"4h"/"1d" (스윙): sl_mult_min=1.5, sl_mult_max=4.0, rr_min=2.0, rr_max=5.0, min_sl_pct=0.5, max_sl_pct=5.0
   - 미지 타임프레임: 데이트레이딩 프리셋 (안전 기본값)

2. scalping_risk.py의 calculate_scalp_risk() 수정 — kelly_fraction과 trade_stats 처리 로직 변경:
   - 현재: kelly_pct > 0이면 min(kelly_pct, risk_per_trade_pct) — Kelly가 아무리 높아도 고정%에 캡됨
   - 변경: kelly_pct > 0이면 effective_risk_pct = kelly_pct (cap 제거, per user decision "고정 2% 완전 대체")
   - RiskManager.position_size_factor()가 드로다운 시 축소하므로 별도 cap 불필요

3. engine/strategy/position_sizer.py 신규 생성:
   - PositionSizeResult dataclass: quantity, risk_amount, position_value, kelly_applied (bool), allocation_weight, size_factor, reason
   - PositionSizer class:
     - __init__(risk_manager: RiskManager | None = None, default_kelly_fraction: float = 0.25, min_trades_for_kelly: int = 20)
     - calculate(df, entry_price, side, capital, config=None, timeframe=None, trade_stats=None, allocation_weight=1.0) -> PositionSizeResult
     - 내부 로직:
       a. timeframe이 주어지면 ScalpRiskConfig.for_timeframe(timeframe) 사용
       b. trade_stats에서 거래 건수 확인: trades >= min_trades_for_kelly이면 kelly_fraction=default_kelly_fraction, 아니면 None
       c. effective_capital = capital * allocation_weight (Risk Parity 배분 반영)
       d. calculate_scalp_risk(df, entry_price, side, effective_capital, config, kelly_fraction, trade_stats) 호출
       e. result.quantity *= risk_manager.position_size_factor() (곱산 합산)
       f. PositionSizeResult 반환

4. tests/test_position_sizer.py 작성 (behavior 항목 기반 TDD)
  </action>
  <verify>
    <automated>cd /home/hwchung/workspace/01_Project/02_stock && .venv/bin/python -m pytest tests/test_position_sizer.py -x -v</automated>
  </verify>
  <done>
    - ScalpRiskConfig.for_timeframe()이 3가지 타임프레임 프리셋을 반환
    - PositionSizer.calculate()가 ATR+Kelly x position_size_factor x allocation_weight 곱산 결과 반환
    - 거래 이력 20건 미만 시 Kelly 미적용, 20건 이상 시 Quarter Kelly 적용
    - 기존 calculate_scalp_risk() Kelly cap 제거됨
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Risk Parity 자본 배분 모듈</name>
  <files>engine/strategy/risk_parity.py, tests/test_risk_parity.py</files>
  <behavior>
    - Test 1: 2개 전략, 동일 변동성 → 동등 배분 (각 0.5)
    - Test 2: 3개 전략, 전략A 변동성 2배 → 전략A 배분 비율이 B,C보다 낮음
    - Test 3: 전략 1개 → 배분 1.0 (max_allocation_per_strategy cap 적용)
    - Test 4: max_allocation_per_strategy=0.4 설정 시 어떤 전략도 0.4 초과 불가
    - Test 5: 수익률 데이터가 비어있거나 NaN만 있으면 균등 배분 fallback
    - Test 6: 공분산 행렬이 singular(역행렬 불가)이면 균등 배분 fallback
  </behavior>
  <action>
1. engine/strategy/risk_parity.py 신규 생성:

```python
@dataclass
class RiskParityConfig:
    max_allocation_per_strategy: float = 0.4  # 전략별 최대 배분 비율
    min_allocation: float = 0.05              # 최소 배분 비율
    lookback_days: int = 60                   # 공분산 추정 윈도우

def calculate_risk_parity_weights(
    returns: dict[str, pd.Series],  # {strategy_id: daily_returns_series}
    config: RiskParityConfig | None = None,
) -> dict[str, float]:
```

2. 구현 상세 (numpy 직접, 10~20줄 핵심):
   - returns dict → DataFrame 변환 (columns = strategy_id)
   - dropna, 유효 행 < 5이면 균등 배분 fallback
   - 공분산 행렬 계산: np.cov(returns_matrix.T)
   - 역행렬 시도: np.linalg.inv(cov) — singular이면 np.linalg.pinv fallback, 그래도 실패 시 균등 배분
   - Risk Parity weights: inv_cov의 행 합 / 전체 합 (역분산 가중)
   - max_allocation_per_strategy cap 적용 후 재정규화 (합 = 1.0)
   - min_allocation 미만 전략은 min_allocation으로 올리고 재정규화

3. tests/test_risk_parity.py 작성 (behavior 항목 기반 TDD)

주의: riskfolio-lib 미사용 (per user decision — numpy만으로 구현)
  </action>
  <verify>
    <automated>cd /home/hwchung/workspace/01_Project/02_stock && .venv/bin/python -m pytest tests/test_risk_parity.py -x -v</automated>
  </verify>
  <done>
    - calculate_risk_parity_weights()가 전략별 리스크 기여 동등 배분 반환
    - 변동성 높은 전략은 낮은 배분, 낮은 전략은 높은 배분
    - max_allocation_per_strategy cap 동작
    - edge case (빈 데이터, singular 행렬) 시 균등 배분 fallback
  </done>
</task>

</tasks>

<verification>
cd /home/hwchung/workspace/01_Project/02_stock && .venv/bin/python -m pytest tests/test_position_sizer.py tests/test_risk_parity.py -x -v
</verification>

<success_criteria>
1. PositionSizer.calculate()가 ATR+Kelly 기반 동적 수량을 반환하고, 고정 2%와 다른 값임을 테스트로 증명
2. 거래 이력 20건 기준으로 Kelly 적용/미적용이 분기됨
3. Risk Parity가 변동성에 반비례하는 배분을 반환
4. 곱산 합산 (ATR+Kelly x position_size_factor x allocation_weight) 동작 확인
5. 모든 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/04-portfolio-risk/04-01-SUMMARY.md`
</output>
