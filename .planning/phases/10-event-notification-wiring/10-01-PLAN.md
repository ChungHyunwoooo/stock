---
phase: 10-event-notification-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/interfaces/bootstrap.py
  - engine/backtest/runner.py
  - engine/strategy/indicator_sweeper.py
  - tests/test_event_notifier.py
autonomous: true
requirements: [MON-01, DISC-01]
must_haves:
  truths:
    - "LifecycleManager 상태 전이 시 Discord에 [LIFECYCLE] 알림이 전송된다"
    - "BacktestRunner.run() 완료 시 Discord에 [BACKTEST] 알림이 전송된다"
    - "IndicatorSweeper sweep 완료 시 각 후보별 [BACKTEST] 알림이 전송된다"
    - "BacktestHistoryPlugin이 Discord 커맨드로 활성화되어 있다"
    - "기존 BacktestRunner() 무인자 호출이 깨지지 않는다"
  artifacts:
    - path: "engine/interfaces/bootstrap.py"
      provides: "EventNotifier 생성 + LifecycleManager callback + orchestrator 주입"
      contains: "EventNotifier"
    - path: "engine/backtest/runner.py"
      provides: "event_notifier optional injection + run() 완료 시 알림"
      contains: "event_notifier"
    - path: "engine/strategy/indicator_sweeper.py"
      provides: "event_notifier optional injection + 후보별 알림"
      contains: "event_notifier"
    - path: "tests/test_event_notifier.py"
      provides: "BacktestRunner + IndicatorSweeper + Bootstrap wiring 통합 테스트"
      contains: "TestBacktestRunnerNotification"
  key_links:
    - from: "engine/interfaces/bootstrap.py"
      to: "engine/notifications/event_notifier.py"
      via: "EventNotifier(notifier) 생성"
      pattern: "EventNotifier\\(notifier\\)"
    - from: "engine/interfaces/bootstrap.py"
      to: "engine/strategy/lifecycle_manager.py"
      via: "add_transition_listener callback"
      pattern: "add_transition_listener"
    - from: "engine/backtest/runner.py"
      to: "engine/notifications/event_notifier.py"
      via: "notify_backtest_complete 호출"
      pattern: "notify_backtest_complete"
    - from: "engine/strategy/indicator_sweeper.py"
      to: "engine/notifications/event_notifier.py"
      via: "notify_backtest_complete 호출"
      pattern: "notify_backtest_complete"
---

<objective>
EventNotifier의 남은 3개 이벤트(lifecycle_transition, backtest_complete, system_error)를 프로덕션 호출 경로에 배선하고, BacktestHistoryPlugin 활성화를 확인한다.

Purpose: MON-01 요구사항 완성 -- 매매 체결/전략 상태 변화/백테스트 결과가 실시간 Discord 알림으로 도착. DISC-01의 알림 측면 완성 -- sweep 후보가 EventNotifier를 통해 통보됨.
Output: bootstrap.py에서 EventNotifier 생성/주입, BacktestRunner + IndicatorSweeper에 event_notifier 파라미터 추가, 통합 테스트.
</objective>

<execution_context>
@/home/hwchung/.claude/get-shit-done/workflows/execute-plan.md
@/home/hwchung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-event-notification-wiring/10-RESEARCH.md

<interfaces>
<!-- EventNotifier -- 4개 메서드, NotificationPort.send_text() 래핑 -->
From engine/notifications/event_notifier.py:
```python
class EventNotifier:
    def __init__(self, notifier: NotificationPort) -> None: ...
    def notify_execution(self, execution: ExecutionRecord) -> bool: ...
    def notify_lifecycle_transition(self, strategy_id: str, from_status: str, to_status: str, reason: str = "") -> bool: ...
    def notify_system_error(self, component: str, error: str, severity: str = "WARNING") -> bool: ...
    def notify_backtest_complete(self, strategy_id: str, symbol: str, sharpe: float | None, total_return: float, max_dd: float | None) -> bool: ...
```

From engine/strategy/lifecycle_manager.py:
```python
# add_transition_listener callback signature: (strategy_id: str, from_status: str, to_status: str) -> None
lifecycle.add_transition_listener(callback)
```

From engine/application/trading/orchestrator.py:
```python
class TradingOrchestrator:
    def __init__(self, store, notifier, broker, *, position_sizer=None, portfolio_risk=None, event_notifier: EventNotifier | None = None): ...
    # L168-169: if self.event_notifier: self.event_notifier.notify_execution(execution)
```

From engine/backtest/runner.py:
```python
class BacktestRunner:
    def __init__(self, slippage_model=None, fee_rate=0.0, auto_save=True, strategy_id=None): ...
    def run(self, strategy, symbol, start, end, timeframe="1d", initial_capital=10_000.0, market=None, regime_enabled=False) -> BacktestResult: ...
```

From engine/strategy/indicator_sweeper.py:
```python
class IndicatorSweeper:
    def __init__(self, config: SweepConfig, registry_path: str = "strategies/registry.json"): ...
    def run(self) -> list[dict]: ...
    def _register_candidates(self, study) -> list[dict]: ...
    def _notify_results(self, candidates: list[dict]) -> None: ...  # DiscordWebhookNotifier 직접 생성
```

From engine/notifications/discord_webhook.py:
```python
class MemoryNotifier:  # 테스트용 인메모리 NotificationPort
    messages: list[str]
    def send_text(self, text: str) -> bool: ...
```

From engine/interfaces/discord/commands/__init__.py:
```python
DEFAULT_COMMAND_PLUGINS = [..., BacktestHistoryPlugin(), ...]  # 이미 등록됨
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap EventNotifier wiring + BacktestRunner event_notifier injection</name>
  <files>engine/interfaces/bootstrap.py, engine/backtest/runner.py, tests/test_event_notifier.py</files>
  <behavior>
    - TestBootstrapWiring: build_trading_runtime() 반환 객체의 orchestrator.event_notifier가 None이 아님
    - TestBacktestRunnerNotification: BacktestRunner(event_notifier=en).run() 완료 후 MemoryNotifier에 [BACKTEST] 메시지 1건 존재
    - TestBacktestRunnerBackwardCompat: BacktestRunner() 무인자 생성이 에러 없이 동작
    - TestBacktestHistoryRegistered: BacktestHistoryPlugin이 DEFAULT_COMMAND_PLUGINS에 포함됨
  </behavior>
  <action>
1. **tests/test_event_notifier.py** -- 테스트 클래스 3개 추가:
   - `TestBootstrapWiring`: `build_trading_runtime()`을 호출하여 `runtime.orchestrator.event_notifier`가 EventNotifier 인스턴스인지 확인. bootstrap이 무거우면 `unittest.mock.patch`로 plugin create를 mock.
   - `TestBacktestRunnerNotification`: MemoryNotifier로 EventNotifier 생성, `BacktestRunner(event_notifier=en)`으로 run() 실행 (간단한 strategy + 짧은 데이터), mem.messages에 "[BACKTEST]" 포함 메시지 1건 확인.
   - `TestBacktestHistoryRegistered`: `from engine.interfaces.discord.commands import DEFAULT_COMMAND_PLUGINS` 임포트 후 BacktestHistoryPlugin 인스턴스가 리스트에 있는지 assert.

2. **engine/backtest/runner.py** -- BacktestRunner에 event_notifier 주입:
   - `__init__`에 `event_notifier: EventNotifier | None = None` 파라미터 추가 (맨 마지막, default=None으로 backward compatible).
   - `self._event_notifier = event_notifier` 저장.
   - TYPE_CHECKING 블록에 `from engine.notifications.event_notifier import EventNotifier` 추가.
   - `run()` 메서드 return 직전 (result 생성 후, `_save_to_db` 후): `if self._event_notifier is not None:` 블록에서 `self._event_notifier.notify_backtest_complete(strategy_id=strategy.name, symbol=result.symbol, sharpe=result.sharpe_ratio, total_return=result.total_return, max_dd=result.max_drawdown)` 호출. try/except Exception으로 감싸고 `logger.warning("backtest completion notification failed: %s", e)`.

3. **engine/interfaces/bootstrap.py** -- build_trading_runtime()에 EventNotifier 배선:
   - import: `from engine.notifications.event_notifier import EventNotifier`
   - `notifier` 생성 직후: `event_notifier = EventNotifier(notifier)` 생성.
   - `lifecycle.add_transition_listener(lambda sid, fr, to: event_notifier.notify_lifecycle_transition(sid, fr, to))` 호출.
   - `TradingOrchestrator(...)` 호출에 `event_notifier=event_notifier` 추가.
   - TradingRuntime dataclass에 `event_notifier: EventNotifier` 필드 추가하고, return에도 포함.

  NOTE: strategy.name은 str (StrategyDefinition.name: str) -- BacktestRunner의 int strategy_id 대신 strategy.name 사용 per research recommendation.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_event_notifier.py -x -v</automated>
  </verify>
  <done>BacktestRunner(event_notifier=...).run() 완료 시 [BACKTEST] 알림 전송, bootstrap에서 EventNotifier가 orchestrator + lifecycle에 연결, BacktestHistoryPlugin 등록 확인, 기존 BacktestRunner() 무인자 호출 유지</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IndicatorSweeper event_notifier injection</name>
  <files>engine/strategy/indicator_sweeper.py, tests/test_event_notifier.py</files>
  <behavior>
    - TestSweeperNotification: IndicatorSweeper(config, event_notifier=en)._register_candidates(study) 호출 후 기준 통과 후보 수만큼 MemoryNotifier에 [BACKTEST] 메시지 존재
    - TestSweeperBackwardCompat: IndicatorSweeper(config) 무인자(event_notifier 없이) 생성이 에러 없이 동작
    - TestSweeperNoCandidate: 기준 미달 trial만 있으면 [BACKTEST] 알림 0건
  </behavior>
  <action>
1. **tests/test_event_notifier.py** -- 테스트 클래스 추가:
   - `TestSweeperNotification`: mock optuna Study (trials=[TrialState.COMPLETE with value >= threshold]), MemoryNotifier + EventNotifier 생성, IndicatorSweeper(config, event_notifier=en)._register_candidates(study) 호출 후 mem.messages에 "[BACKTEST]" 메시지가 후보 수와 같은지 확인. LifecycleManager.register를 mock하여 실제 파일 I/O 방지.
   - `TestSweeperBackwardCompat`: IndicatorSweeper(config) (event_notifier 파라미터 없이) 생성 확인.
   - `TestSweeperNoCandidate`: 모든 trial이 threshold 미달 -> _register_candidates 후 mem.messages가 빈 리스트.

2. **engine/strategy/indicator_sweeper.py** -- event_notifier 주입:
   - `__init__`에 `event_notifier: EventNotifier | None = None` 파라미터 추가 (맨 마지막, default=None).
   - `self._event_notifier = event_notifier` 저장.
   - TYPE_CHECKING 블록 추가: `from engine.notifications.event_notifier import EventNotifier`.
   - `_register_candidates()` 메서드 내, 각 후보 등록 직후 (`candidates.append` 후): `if self._event_notifier is not None:` 블록에서 `self._event_notifier.notify_backtest_complete(strategy_id=strategy_id, symbol=self._config.symbols[0], sharpe=trial.value, total_return=0.0, max_dd=None)` 호출. try/except Exception으로 감싸고 `logger.warning("sweep candidate notification failed: %s", e)`.
   - 기존 `_notify_results()` (DiscordWebhookNotifier 직접 사용)는 유지 -- 역할 분리: _notify_results는 요약 알림, EventNotifier는 개별 후보 알림.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_event_notifier.py -x -v</automated>
  </verify>
  <done>IndicatorSweeper(event_notifier=...) 후보 등록 시 개별 [BACKTEST] 알림 전송, 기존 요약 알림(_notify_results) 유지, event_notifier 없이 생성 가능</done>
</task>

</tasks>

<verification>
1. 전체 테스트 스위트 통과: `.venv/bin/python -m pytest`
2. EventNotifier 테스트 전체 통과: `.venv/bin/python -m pytest tests/test_event_notifier.py -v`
3. bootstrap.py에서 EventNotifier가 생성되고 orchestrator + lifecycle에 주입됨
4. BacktestRunner, IndicatorSweeper 모두 event_notifier=None 기본값으로 backward compatible
</verification>

<success_criteria>
- build_trading_runtime()이 EventNotifier를 생성하여 orchestrator.event_notifier에 주입한다
- LifecycleManager.add_transition_listener()로 상태 전이 시 notify_lifecycle_transition이 호출된다
- BacktestRunner.run() 완료 시 notify_backtest_complete가 호출된다
- IndicatorSweeper._register_candidates()에서 후보별 notify_backtest_complete가 호출된다
- BacktestHistoryPlugin이 DEFAULT_COMMAND_PLUGINS에 등록되어 있다
- 기존 코드의 무인자 호출이 모두 유지된다 (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/10-event-notification-wiring/10-01-SUMMARY.md`
</output>
