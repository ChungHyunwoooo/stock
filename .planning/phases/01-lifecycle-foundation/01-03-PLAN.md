---
phase: 01-lifecycle-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - strategies/ref_rsi_divergence/definition.json
  - strategies/ref_rsi_divergence/research.md
  - strategies/registry.json
  - tests/test_lifecycle.py
autonomous: true
requirements:
  - LIFE-04

must_haves:
  truths:
    - "레퍼런스 전략 1개가 strategies/ref_rsi_divergence/ 디렉토리에 definition.json + research.md로 존재한다"
    - "research.md에 출처(논문/URL), 전략 로직 요약, 백테스트 결과 요약이 포함되어 있다"
    - "definition.json이 StrategyDefinition pydantic model로 유효하게 파싱된다"
    - "registry.json에 ref_rsi_divergence가 status=draft로 등록되어 있다"
    - "registry.json의 해당 항목에 status_history 첫 기록이 {from: null, to: draft} 형태이다"
  artifacts:
    - path: "strategies/ref_rsi_divergence/definition.json"
      provides: "RSI Divergence 레퍼런스 전략 정의"
    - path: "strategies/ref_rsi_divergence/research.md"
      provides: "전략 출처, 로직 요약, 백테스트 결과"
      min_lines: 20
  key_links:
    - from: "strategies/ref_rsi_divergence/definition.json"
      to: "engine/schema.py"
      via: "StrategyDefinition model validation"
      pattern: "StrategyDefinition"
    - from: "strategies/registry.json"
      to: "strategies/ref_rsi_divergence/definition.json"
      via: "definition path reference"
      pattern: "ref_rsi_divergence"
---

<objective>
논문 기반 RSI Divergence 레퍼런스 전략을 JSON StrategyDefinition으로 변환하여 draft 상태로 등록하고, 이 워크플로우 자체를 재현 가능한 프로세스로 증명한다.

Purpose: 전략 임포트 워크플로우의 end-to-end 증명. 향후 논문/커뮤니티 전략을 동일 프로세스로 등록할 수 있음을 보인다.
Output: 레퍼런스 전략 파일 (definition.json + research.md), registry.json에 draft 등록
</objective>

<execution_context>
@/home/hwchung/.claude/get-shit-done/workflows/execute-plan.md
@/home/hwchung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-lifecycle-foundation/01-CONTEXT.md
@.planning/phases/01-lifecycle-foundation/01-RESEARCH.md
@.planning/phases/01-lifecycle-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output (engine/strategy/lifecycle_manager.py) -->
```python
from engine.strategy.lifecycle_manager import LifecycleManager

# manager.register(entry: dict) -> dict
# Registers a new strategy as draft, initializes status_history
```

<!-- From engine/schema.py (StrategyDefinition contract) -->
```python
class StrategyDefinition(BaseModel):
    name: str
    version: str = "1.0"
    description: str = ""
    markets: list[MarketType]
    direction: Direction
    timeframes: list[str]
    indicators: list[IndicatorDef]
    entry: ConditionGroup
    exit: ConditionGroup
    risk: RiskParams = Field(default_factory=RiskParams)
    regime: RegimeConfig = Field(default_factory=RegimeConfig)
    status: StrategyStatus = StrategyStatus.draft
    created_at: date | None = None
```

<!-- From 01-RESEARCH.md: research.md template -->
```markdown
# {전략명} -- Research

## 출처
- **논문/URL**: {링크}
- **저자/커뮤니티**: {출처}

## 전략 로직 요약
- **진입 조건**: {description}
- **청산 조건**: {description}
- **사용 지표**: {indicators}
- **타임프레임**: {timeframes}
- **방향**: {long/short/both}

## 백테스트 결과 요약
| 항목 | 값 |
|------|-----|
| 기간 | {period} |
| 총 수익률 | {return}% |
| 승률 | {win_rate}% |
| Sharpe | {sharpe} |
| 최대 DD | {max_dd}% |

## 메모
{추가 관찰 사항}
```

<!-- registry.json entry format for new strategy -->
```json
{
  "id": "ref_rsi_divergence",
  "name": "RSI Divergence",
  "status": "draft",
  "direction": ["LONG", "SHORT"],
  "timeframe": ["1h"],
  "regime": ["ALL"],
  "definition": "strategies/ref_rsi_divergence/definition.json",
  "status_history": [
    {"from": null, "to": "draft", "date": "...", "reason": "레퍼런스 전략 초기 등록"}
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RSI Divergence 레퍼런스 전략 생성 + draft 등록</name>
  <files>strategies/ref_rsi_divergence/definition.json, strategies/ref_rsi_divergence/research.md, strategies/registry.json</files>
  <action>
1. strategies/ref_rsi_divergence/ 디렉토리 생성

2. strategies/ref_rsi_divergence/research.md 작성 (01-RESEARCH.md 템플릿 준수):
   - 출처: Andrew Cardwell의 RSI Divergence 이론 (Constance Brown, "Technical Analysis for the Trading Professional", 1999). URL: 공개 학술 자료 기반
   - 전략 로직 요약:
     - 진입 조건: RSI 14가 30 이하에서 bullish divergence (가격은 lower low, RSI는 higher low) 발생 시 LONG. RSI 70 이상에서 bearish divergence (가격은 higher high, RSI는 lower high) 발생 시 SHORT.
     - 청산 조건: RSI가 50 크로스 시 또는 ATR 기반 SL 도달 시
     - 사용 지표: RSI(14), ATR(14)
     - 타임프레임: 1h
     - 방향: both (long + short)
   - 백테스트 결과 요약: 논문 기반 이론적 수치 (실 백테스트는 Phase 2에서 수행)
     - 기간: 이론적 참고치
     - 참고 승률: ~55-60% (문헌 기준)
     - 참고 Sharpe: ~1.0-1.5 (문헌 기준)
     - 주의: Phase 2 백테스트로 실측 검증 필요
   - 메모: "Phase 1 워크플로우 증명용 레퍼런스. Phase 2에서 실 백테스트 수행 예정."

3. strategies/ref_rsi_divergence/definition.json 작성:
   - StrategyDefinition 스키마 준수
   - name: "RSI Divergence"
   - version: "1.0"
   - description: "RSI divergence 기반 추세 반전 전략 (Cardwell 이론)"
   - markets: ["crypto_futures"]
   - direction: "both"
   - timeframes: ["1h"]
   - indicators: RSI(14), ATR(14) 정의
   - entry: RSI crosses_below 30 (simplified — divergence 로직은 Phase 2에서 condition_evaluator 확장)
   - exit: RSI crosses_above 50 (LONG exit) / crosses_below 50 (SHORT exit)
   - risk: 기본값 사용
   - regime: {"mode": "ALL"}
   - status: "draft"
   - 참고: 이 definition.json은 워크플로우 증명 목적. RSI divergence의 정밀한 조건 표현은 Phase 2 condition_evaluator 확장 후 업데이트.

4. LifecycleManager.register()를 사용하여 registry.json에 등록:
   - Python 스크립트 또는 직접 registry.json 편집
   - entry 구조: {id, name, status: "draft", direction, timeframe, regime, definition path, status_history}
   - status_history 첫 항목: {from: null, to: "draft", date: ISO timestamp, reason: "레퍼런스 전략 초기 등록"}
   - 기존 16개 전략 항목은 절대 수정하지 않음 — 새 항목을 strategies 배열 끝에 추가

5. definition.json이 StrategyDefinition으로 유효하게 파싱되는지 확인
  </action>
  <verify>
    <automated>.venv/bin/python -c "from engine.schema import StrategyDefinition; import json; d=json.loads(open('strategies/ref_rsi_divergence/definition.json').read()); StrategyDefinition.model_validate(d); print('OK')" && .venv/bin/python -m pytest tests/test_lifecycle.py::test_reference_strategy_valid tests/test_lifecycle.py::test_register_strategy -x -v 2>/dev/null || .venv/bin/python -m pytest tests/test_lifecycle.py -x -v -k "reference or register"</automated>
  </verify>
  <done>
    - strategies/ref_rsi_divergence/definition.json이 StrategyDefinition으로 유효하게 파싱된다
    - strategies/ref_rsi_divergence/research.md에 출처, 로직 요약, 백테스트 요약이 포함된다
    - registry.json에 ref_rsi_divergence가 status=draft, status_history 포함하여 등록된다
    - 기존 16개 전략 항목이 변경되지 않았다
    - 전체 pytest 스위트 통과
  </done>
</task>

</tasks>

<verification>
.venv/bin/python -c "
from engine.schema import StrategyDefinition
import json
d = json.loads(open('strategies/ref_rsi_divergence/definition.json').read())
s = StrategyDefinition.model_validate(d)
assert s.status.value == 'draft', f'Expected draft, got {s.status.value}'
print(f'Validated: {s.name} v{s.version} status={s.status.value}')
"
.venv/bin/python -c "
import json
reg = json.loads(open('strategies/registry.json').read())
entries = [e for e in reg['strategies'] if e['id'] == 'ref_rsi_divergence']
assert len(entries) == 1, 'ref_rsi_divergence not found in registry'
e = entries[0]
assert e['status'] == 'draft', f'Expected draft, got {e[\"status\"]}'
assert 'status_history' in e, 'Missing status_history'
assert e['status_history'][0]['to'] == 'draft', 'First history entry should be to=draft'
print(f'Registry OK: {e[\"id\"]} status={e[\"status\"]} history={len(e[\"status_history\"])} entries')
"
.venv/bin/python -m pytest  # 전체 스위트 회귀 확인
</verification>

<success_criteria>
- 레퍼런스 전략 definition.json이 StrategyDefinition 스키마로 유효하다
- research.md에 출처, 로직 요약, 백테스트 결과 요약이 모두 포함된다
- registry.json에 draft 상태로 등록되어 있고 status_history가 초기화되어 있다
- 기존 전략 항목이 전혀 변경되지 않았다
- 전체 pytest 스위트가 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/01-lifecycle-foundation/01-03-SUMMARY.md`
</output>
