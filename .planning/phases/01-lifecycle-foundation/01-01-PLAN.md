---
phase: 01-lifecycle-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - engine/schema.py
  - engine/strategy/lifecycle_manager.py
  - tests/test_lifecycle.py
  - tests/test_schema.py
autonomous: true
requirements:
  - LIFE-01

must_haves:
  truths:
    - "draft->testing->paper->active->archived 정방향 전이가 성공한다"
    - "active->paper, testing->draft, archived->draft 역전이가 성공한다"
    - "허용되지 않은 전이(예: active->draft, draft->active)를 시도하면 InvalidTransitionError가 발생한다"
    - "전이 시 status_history 배열에 {from, to, date, reason} 기록이 추가된다"
    - "registry.json 쓰기가 원자적이어서 중간 실패 시 기존 데이터가 보존된다"
  artifacts:
    - path: "engine/strategy/lifecycle_manager.py"
      provides: "LifecycleManager FSM + InvalidTransitionError + StrategyNotFoundError"
      exports: ["LifecycleManager", "InvalidTransitionError", "StrategyNotFoundError", "ALLOWED_TRANSITIONS"]
    - path: "engine/schema.py"
      provides: "StrategyStatus enum with paper member"
      contains: "paper"
    - path: "tests/test_lifecycle.py"
      provides: "LifecycleManager unit tests"
      min_lines: 80
  key_links:
    - from: "engine/strategy/lifecycle_manager.py"
      to: "engine/schema.py"
      via: "StrategyStatus enum import"
      pattern: "from engine\\.schema import StrategyStatus"
    - from: "engine/strategy/lifecycle_manager.py"
      to: "strategies/registry.json"
      via: "JSON load/save"
      pattern: "registry_path"
---

<objective>
StrategyStatus enum에 paper를 추가하고, 전이 규칙을 강제하는 LifecycleManager를 TDD로 구현한다.

Purpose: 전략 상태머신의 핵심 도메인 로직. draft 전략이 gate 없이 실매매에 진입하는 사고를 코드 수준에서 차단한다.
Output: engine/strategy/lifecycle_manager.py (순수 도메인 서비스), 수정된 StrategyStatus enum, 전이 규칙 테스트
</objective>

<execution_context>
@/home/hwchung/.claude/get-shit-done/workflows/execute-plan.md
@/home/hwchung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-lifecycle-foundation/01-CONTEXT.md
@.planning/phases/01-lifecycle-foundation/01-RESEARCH.md

<interfaces>
<!-- Executor needs these existing contracts -->

From engine/schema.py (line 26):
```python
class StrategyStatus(str, Enum):
    draft = "draft"
    testing = "testing"
    active = "active"
    archived = "archived"
```

From strategies/registry.json (structure):
```json
{
  "strategies": [
    {
      "id": "dante_staircase",
      "name": "세력계단",
      "status": "active",
      "direction": ["LONG"],
      "timeframe": ["1h", "4h"],
      "regime": ["BULL"],
      "definition": "strategies/dante_staircase/definition.json"
    }
  ]
}
```

From tests/test_schema.py (line 71-80, must update):
```python
def test_strategy_status_enum():
    statuses = ["draft", "testing", "active", "archived"]
    for s in statuses:
        assert StrategyStatus(s).value == s
    assert len(StrategyStatus) == 4
    with pytest.raises(ValueError):
        StrategyStatus("invalid_status")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StrategyStatus enum 확장 + LifecycleManager TDD 구현</name>
  <files>engine/schema.py, engine/strategy/lifecycle_manager.py, tests/test_lifecycle.py, tests/test_schema.py</files>
  <behavior>
    - test_forward_transitions: draft->testing, testing->paper, paper->active, active->archived 각각 성공하고 entry["status"]가 target과 일치
    - test_allowed_reverse_transitions: active->paper, testing->draft, archived->draft 각각 성공
    - test_invalid_transitions: draft->active, draft->paper, paper->testing, archived->active 등 불허 전이 시 InvalidTransitionError 발생
    - test_transition_history: 전이 후 entry["status_history"][-1]이 {from, to, date, reason} 포함. date는 ISO format 문자열
    - test_strategy_not_found: 존재하지 않는 strategy_id로 전이 시도 시 StrategyNotFoundError 발생
    - test_atomic_write: registry.json 쓰기 중 예외 발생 시 원본 파일이 보존됨 (tmp_path 사용)
    - test_register_strategy: 새 전략을 draft로 등록하면 registry.json에 추가되고 status_history 첫 항목이 {from: null, to: "draft"} 포함
    - test_get_strategy: strategy_id로 전략 항목을 조회하면 dict 반환, 없으면 StrategyNotFoundError
    - test_list_by_status: 특정 status로 필터링하면 해당 status 전략만 반환
    - test_deprecated_strategies_ignored: status가 "deprecated"인 기존 전략에 대해 전이를 시도하면 InvalidTransitionError (ALLOWED_TRANSITIONS에 deprecated 키 없음)
    - test_strategy_status_enum (test_schema.py 수정): statuses 리스트에 "paper" 추가, len == 5
  </behavior>
  <action>
RED phase:
1. engine/schema.py: StrategyStatus enum에 `paper = "paper"` 추가 (testing과 active 사이에 배치)
2. tests/test_schema.py: test_strategy_status_enum 수정 -- statuses 리스트에 "paper" 추가, assert len == 5
3. tests/test_lifecycle.py 생성: behavior에 명시된 모든 테스트를 작성. tmp_path fixture로 임시 registry.json 생성하여 LifecycleManager(registry_path=tmp_path/"registry.json") 테스트. 테스트 실행 시 import 실패 확인.

GREEN phase:
4. engine/strategy/lifecycle_manager.py 생성:
   - ALLOWED_TRANSITIONS dict[StrategyStatus, set[StrategyStatus]] 정의
     - draft: {testing}
     - testing: {draft, paper}
     - paper: {active}
     - active: {paper, archived}
     - archived: {draft}
   - InvalidTransitionError(Exception), StrategyNotFoundError(Exception) 정의
   - LifecycleManager class:
     - __init__(self, registry_path: str | Path = "strategies/registry.json")
     - _load() -> dict: json.loads로 registry.json 읽기
     - _save(data: dict) -> None: tempfile + rename 원자적 쓰기 (01-RESEARCH.md Pattern 4)
     - _find_entry(registry: dict, strategy_id: str) -> dict: strategies 배열에서 id 매칭, 없으면 StrategyNotFoundError
     - transition(self, strategy_id: str, target: str, reason: str = "") -> dict: 전이 수행 + status_history 기록
     - register(self, entry: dict) -> dict: 새 전략을 draft로 등록 (status="draft" 강제, status_history 초기화)
     - get_strategy(self, strategy_id: str) -> dict: 전략 항목 조회
     - list_by_status(self, status: str | None = None) -> list[dict]: 상태별 필터링
   - LifecycleManager는 순수 도메인 서비스 -- Discord/API를 직접 호출하지 않음
   - 기존 deprecated 전략은 ALLOWED_TRANSITIONS에 키가 없으므로 전이 불가 (의도된 동작)
   - `from __future__ import annotations` 첫 줄, logger = logging.getLogger(__name__)

REFACTOR phase:
5. 코드 정리, 불필요한 중복 제거

모든 테스트가 통과하면 완료.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_lifecycle.py tests/test_schema.py::test_strategy_status_enum -x -v</automated>
  </verify>
  <done>
    - StrategyStatus enum에 paper 포함 (5개 상태)
    - LifecycleManager가 모든 정방향/역방향 전이를 정확히 처리
    - 불허 전이 시 InvalidTransitionError 발생
    - status_history에 전이 이력이 기록됨
    - registry.json 원자적 쓰기 동작
    - register()로 새 전략 draft 등록 가능
    - 기존 deprecated 전략 전이 불가
    - 전체 테스트 스위트(.venv/bin/python -m pytest) 통과
  </done>
</task>

</tasks>

<verification>
.venv/bin/python -m pytest tests/test_lifecycle.py tests/test_schema.py -x -v
.venv/bin/python -m pytest  # 전체 스위트 회귀 확인
</verification>

<success_criteria>
- LifecycleManager가 7개 허용 전이(정방향 4 + 역방향 3)를 정확히 처리한다
- 불허 전이를 시도하면 InvalidTransitionError가 즉시 발생한다
- 전이 이력이 status_history 배열에 누적 기록된다
- registry.json 원자적 쓰기로 데이터 안전성이 보장된다
- 전체 pytest 스위트가 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/01-lifecycle-foundation/01-01-SUMMARY.md`
</output>
