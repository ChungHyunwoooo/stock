---
phase: 08-monitoring-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - engine/strategy/indicator_sweeper.py
  - engine/interfaces/dashboard/pages/sweep_progress.py
  - engine/interfaces/dashboard/pages/settings_editor.py
  - engine/interfaces/streamlit_dashboard.py
  - tests/test_dashboard.py
autonomous: true
requirements:
  - MON-03

must_haves:
  truths:
    - "대시보드에서 전략 설정(임계치, 필터 on/off)을 변경하면 다음 스캔 사이클부터 반영된다"
    - "자동 탐색 큐의 진행 현황(완료/전체 trial 수, 현재 Sharpe)을 실시간으로 볼 수 있다"
  artifacts:
    - path: "engine/interfaces/dashboard/pages/sweep_progress.py"
      provides: "Sweep queue progress panel"
      min_lines: 20
    - path: "engine/interfaces/dashboard/pages/settings_editor.py"
      provides: "Strategy config edit UI"
      min_lines: 30
    - path: "engine/strategy/indicator_sweeper.py"
      provides: "sweep_status.json writer in run loop"
      contains: "sweep_status.json"
    - path: "tests/test_dashboard.py"
      provides: "sweep progress + config edit tests"
      contains: "test_sweep_progress"
  key_links:
    - from: "engine/strategy/indicator_sweeper.py"
      to: "state/sweep_status.json"
      via: "JSON write after each trial"
      pattern: "sweep_status"
    - from: "engine/interfaces/dashboard/pages/sweep_progress.py"
      to: "state/sweep_status.json"
      via: "JSON read"
      pattern: "sweep_status"
    - from: "engine/interfaces/dashboard/pages/settings_editor.py"
      to: "strategies/{id}/definition.json"
      via: "atomic tempfile+rename write"
      pattern: "definition\\.json"
    - from: "engine/interfaces/streamlit_dashboard.py"
      to: "engine/interfaces/dashboard/pages/"
      via: "st.Page additions"
      pattern: "sweep_progress|settings_editor"
---

<objective>
Sweep 진행 현황 패널 + 전략 설정 변경 UI + IndicatorSweeper 상태 파일 출력

Purpose: 대시보드에서 자동 탐색 현황을 실시간 모니터링하고, 전략 설정을 웹에서 직접 변경 가능하게 함
Output: sweep_status.json writer, sweep progress page, settings editor page, 추가 테스트
</objective>

<execution_context>
@/home/hwchung/.claude/get-shit-done/workflows/execute-plan.md
@/home/hwchung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-monitoring-dashboard/08-RESEARCH.md
@.planning/phases/08-monitoring-dashboard/08-01-SUMMARY.md

@engine/strategy/indicator_sweeper.py
@engine/interfaces/streamlit_dashboard.py
@engine/interfaces/dashboard/data_service.py
@engine/strategy/lifecycle_manager.py

<interfaces>
<!-- From Plan 01 outputs -->

From engine/interfaces/dashboard/data_service.py (created in 08-01):
```python
class DashboardDataService:
    def get_lifecycle_summary(self) -> list[dict]
    def get_lifecycle_counts(self) -> dict[str, int]
    def get_portfolio_summary(self, session) -> dict
    def get_strategy_pnl(self, session, strategy_id: str) -> dict
    def get_system_health(self) -> dict
    def get_open_positions(self, session) -> list[dict]
    def get_closed_trades(self, session, limit=50) -> list[dict]
```

From engine/strategy/indicator_sweeper.py:
```python
class IndicatorSweeper:
    def __init__(self, config: SweepConfig, registry_path: str = "strategies/registry.json") -> None
    def run(self) -> list[dict]
    # _objective(self, trial) is called per-trial by Optuna
```

From engine/strategy/lifecycle_manager.py:
```python
class LifecycleManager:
    def list_by_status(self, status: str | None = None) -> list[dict]
    def get_strategy(self, strategy_id: str) -> dict
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IndicatorSweeper sweep_status.json writer + sweep/config 테스트</name>
  <files>
    engine/strategy/indicator_sweeper.py,
    tests/test_dashboard.py
  </files>
  <behavior>
    - test_sweep_progress: state/sweep_status.json 파일이 존재하면 completed, total, best_sharpe, candidates_found 키를 포함하는 딕셔너리 반환. 파일 미존재 시 None 반환.
    - test_config_edit: definition.json을 읽어 수정 후 atomic write하면 변경된 값이 반영됨. 원본 파일이 깨지지 않음.
    - test_sweep_status_writer: _write_sweep_status 호출 시 state/sweep_status.json에 올바른 JSON 기록.
  </behavior>
  <action>
    1. `tests/test_dashboard.py`에 3개 테스트 추가:
       - `test_sweep_progress`: 임시 sweep_status.json 생성 후 읽기 검증
       - `test_config_edit`: 임시 definition.json 생성 -> 값 수정 -> atomic write -> 검증
       - `test_sweep_status_writer`: IndicatorSweeper._write_sweep_status 호출 -> 파일 내용 검증

    2. `engine/strategy/indicator_sweeper.py` 수정:
       - `_write_sweep_status(self, completed: int, total: int, best_sharpe: float, candidates_found: int) -> None` 메서드 추가
         - `state/sweep_status.json`에 JSON 기록: {"completed": N, "total": N, "best_sharpe": X, "candidates_found": N, "updated_at": ISO timestamp}
         - state/ 디렉토리 없으면 생성
       - `_objective` 메서드 내부에서 trial 완료 후 `_write_sweep_status` 호출 (study.trials에서 현재까지 완료 수 계산)
         - completed = trial.number + 1 (0-indexed)
         - total = self._config.n_trials
         - best_sharpe = max of completed trial values (None 제외)
       - `run()` 메서드에서 study.optimize 완료 후 최종 상태 기록 + candidates_found 반영
       - `run()` 완료 시 sweep_status.json 삭제하지 않음 (대시보드에서 마지막 결과 확인용)

    3. DashboardDataService에 `get_sweep_status(self) -> dict | None` 메서드 추가:
       - `state/sweep_status.json` 읽기, 파일 없으면 None 반환
       - JSONDecodeError 시 None 반환 (부분 기록 방어)
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_dashboard.py -x -v</automated>
  </verify>
  <done>
    - IndicatorSweeper가 trial마다 sweep_status.json 업데이트
    - DashboardDataService.get_sweep_status()가 sweep 상태 반환
    - 3개 추가 테스트 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep Progress 페이지 + Settings Editor 페이지 + 네비게이션 확장</name>
  <files>
    engine/interfaces/dashboard/pages/sweep_progress.py,
    engine/interfaces/dashboard/pages/settings_editor.py,
    engine/interfaces/streamlit_dashboard.py
  </files>
  <action>
    1. `engine/interfaces/dashboard/pages/sweep_progress.py`:
       - DashboardDataService.get_sweep_status()로 상태 로드
       - 상태 없으면 st.info("No active sweep") 표시
       - 상태 있으면:
         - st.columns(3)로 Progress (completed/total), Best Sharpe, Candidates Found 표시
         - st.progress(completed / max(total, 1))로 프로그레스 바
         - updated_at 표시
       - `@st.fragment(run_every=10)` 으로 10초 자동 갱신 (sweep은 빠르게 변하므로 30초보다 짧게)

    2. `engine/interfaces/dashboard/pages/settings_editor.py`:
       - DashboardDataService.get_lifecycle_summary()로 전략 목록 로드
       - st.selectbox로 전략 선택 (id 기준)
       - 선택한 전략의 `strategies/{id}/definition.json` 로드
       - st.form("settings_form") 내부:
         - risk 섹션: stop_loss_pct (st.number_input), take_profit_pct (st.number_input)
         - 존재하는 필드만 표시 (KeyError 방어)
       - form submit 시 atomic write (tempfile + rename, LifecycleManager._save 패턴 동일)
       - 성공 시 st.success("Saved -- applied from next scan cycle")
       - definition.json 미존재 시 st.warning 표시
       - interactive widget이므로 @st.fragment(run_every=N) 사용 금지 (pitfall 3 준수)

    3. `engine/interfaces/streamlit_dashboard.py` 수정:
       - st.navigation에 2개 페이지 추가:
         - Sweep Queue (sweep_progress.py)
         - Settings (settings_editor.py)
       - 총 5개 페이지: Lifecycle, Portfolio PnL, System Health, Sweep Queue, Settings
  </action>
  <verify>
    <automated>.venv/bin/python -c "from engine.interfaces.dashboard.pages import sweep_progress, settings_editor; print('All page imports OK')" && .venv/bin/python -m pytest tests/test_dashboard.py -x -v</automated>
  </verify>
  <done>
    - Sweep Queue 페이지에서 sweep_status.json 기반 진행 현황 표시 (10초 갱신)
    - Settings 페이지에서 전략 설정 변경 가능 (atomic write)
    - streamlit_dashboard.py에서 5개 페이지 네비게이션 제공
    - 모든 테스트 통과
  </done>
</task>

</tasks>

<verification>
1. `.venv/bin/python -m pytest tests/test_dashboard.py -x -v` -- 전체 대시보드 테스트 통과
2. `.venv/bin/python -c "from engine.interfaces.dashboard.pages import sweep_progress, settings_editor"` -- import 성공
3. `.venv/bin/python -m pytest` -- 전체 테스트 스위트 미파괴
</verification>

<success_criteria>
- IndicatorSweeper가 trial마다 state/sweep_status.json에 진행 상태 기록
- Sweep Queue 페이지에서 completed/total, best_sharpe, candidates 표시 (10초 갱신)
- Settings 페이지에서 전략의 risk 파라미터를 변경하고 definition.json에 atomic write
- 기존 IndicatorSweeper 동작 미파괴 (backward compatible)
- 전체 테스트 스위트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/08-monitoring-dashboard/08-02-SUMMARY.md`
</output>
