---
phase: 08-monitoring-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/interfaces/dashboard/__init__.py
  - engine/interfaces/dashboard/data_service.py
  - engine/interfaces/dashboard/pages/__init__.py
  - engine/interfaces/dashboard/pages/lifecycle_view.py
  - engine/interfaces/dashboard/pages/portfolio_pnl.py
  - engine/interfaces/dashboard/components/__init__.py
  - engine/interfaces/dashboard/components/metrics_bar.py
  - engine/interfaces/dashboard/components/pnl_chart.py
  - engine/interfaces/dashboard/components/health_indicator.py
  - engine/interfaces/streamlit_dashboard.py
  - tests/test_dashboard.py
autonomous: true
requirements:
  - MON-03

must_haves:
  truths:
    - "대시보드에서 전체 전략의 현재 상태(draft/testing/paper/active/archived)와 단계별 현황을 볼 수 있다"
    - "실시간 포지션, 전략별 PnL 차트, 시스템 상태를 30초 이내 갱신으로 확인할 수 있다"
  artifacts:
    - path: "engine/interfaces/dashboard/data_service.py"
      provides: "Dashboard data access layer wrapping existing repos"
      exports: ["DashboardDataService"]
    - path: "engine/interfaces/dashboard/pages/lifecycle_view.py"
      provides: "Strategy lifecycle status grid page"
      min_lines: 20
    - path: "engine/interfaces/dashboard/pages/portfolio_pnl.py"
      provides: "Portfolio PnL + strategy performance charts page"
      min_lines: 30
    - path: "engine/interfaces/streamlit_dashboard.py"
      provides: "Multi-page navigation entry point"
      contains: "st.navigation"
    - path: "tests/test_dashboard.py"
      provides: "DashboardDataService unit tests"
      min_lines: 40
  key_links:
    - from: "engine/interfaces/dashboard/data_service.py"
      to: "engine/core/repository.py"
      via: "TradeRepository import"
      pattern: "TradeRepository"
    - from: "engine/interfaces/dashboard/data_service.py"
      to: "engine/strategy/lifecycle_manager.py"
      via: "LifecycleManager import"
      pattern: "LifecycleManager"
    - from: "engine/interfaces/dashboard/data_service.py"
      to: "engine/core/json_store.py"
      via: "JsonRuntimeStore import"
      pattern: "JsonRuntimeStore"
    - from: "engine/interfaces/streamlit_dashboard.py"
      to: "engine/interfaces/dashboard/pages/"
      via: "st.navigation + st.Page"
      pattern: "st\\.Page"
---

<objective>
DashboardDataService 데이터 계층 + 멀티페이지 Streamlit 대시보드 (Lifecycle 뷰 + Portfolio PnL + 시스템 헬스)

Purpose: 전략 파이프라인 전체 상태를 웹에서 한눈에 확인할 수 있는 대시보드의 핵심 기능 구현
Output: DashboardDataService, 3개 페이지(lifecycle/portfolio/health), 테스트, 멀티페이지 진입점
</objective>

<execution_context>
@/home/hwchung/.claude/get-shit-done/workflows/execute-plan.md
@/home/hwchung/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-monitoring-dashboard/08-RESEARCH.md

@engine/interfaces/streamlit_dashboard.py
@engine/strategy/lifecycle_manager.py
@engine/core/json_store.py
@engine/core/repository.py
@engine/strategy/performance_monitor.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From engine/strategy/lifecycle_manager.py:
```python
class LifecycleManager:
    def __init__(self, registry_path: str | Path = "strategies/registry.json") -> None
    def list_by_status(self, status: str | None = None) -> list[dict]
    def get_strategy(self, strategy_id: str) -> dict
```

From engine/core/repository.py:
```python
class TradeRepository:
    def list_open(self, session, symbol=None, strategy_name=None, broker=None) -> list[TradeRecord]
    def list_closed(self, session, symbol=None, strategy_name=None, broker=None, limit=100) -> list[TradeRecord]
    def summary(self, session, strategy_name=None, broker=None) -> dict

class PaperRepository:
    def get_daily_snapshots(self, session, strategy_id, limit=90) -> list[PaperPnlSnapshot]
```

From engine/core/json_store.py:
```python
class JsonRuntimeStore:
    def __init__(self, path: str | Path = "state/runtime_state.json") -> None
    def load(self) -> TradingRuntimeState

class TradingRuntimeState:
    mode: TradingMode
    paused: bool
    paused_strategies: set[str]
    updated_at: str
```

From engine/strategy/performance_monitor.py:
```python
@dataclass
class PerformanceSnapshot:
    strategy_id: str
    rolling_sharpe: float | None
    rolling_win_rate: float | None
    baseline_sharpe: float | None
    baseline_win_rate: float | None
```

From engine/core/database.py:
```python
def init_db() -> None
def get_session() -> Generator[Session, None, None]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DashboardDataService + 테스트</name>
  <files>
    engine/interfaces/dashboard/__init__.py,
    engine/interfaces/dashboard/data_service.py,
    engine/interfaces/dashboard/pages/__init__.py,
    engine/interfaces/dashboard/components/__init__.py,
    tests/test_dashboard.py
  </files>
  <behavior>
    - test_lifecycle_data: list_by_status(None) 호출 시 전체 전략 리스트 반환, status별 그룹핑 카운트 딕셔너리 반환
    - test_portfolio_data: get_portfolio_summary() 호출 시 총 PnL, 승률, 거래 수 포함 딕셔너리 반환
    - test_system_health: get_system_health() 호출 시 mode, paused, paused_strategies, updated_at 포함 딕셔너리 반환
    - test_strategy_pnl: get_strategy_pnl(session, strategy_id) 호출 시 TradeRepository.summary() 결과 반환
  </behavior>
  <action>
    1. `engine/interfaces/dashboard/` 디렉토리 + `__init__.py` 생성 (pages/, components/ 포함)
    2. `tests/test_dashboard.py` 작성 -- DashboardDataService의 4개 메서드를 mock 기반으로 테스트:
       - LifecycleManager.list_by_status를 mock하여 lifecycle_summary 검증
       - TradeRepository.summary를 mock하여 portfolio/strategy PnL 검증
       - JsonRuntimeStore.load를 mock하여 system_health 검증
    3. `engine/interfaces/dashboard/data_service.py` 구현:
       - `DashboardDataService` 클래스: 생성자에서 TradeRepository, LifecycleManager, JsonRuntimeStore 인스턴스화
       - `get_lifecycle_summary() -> list[dict]`: LifecycleManager.list_by_status(None) 호출
       - `get_lifecycle_counts() -> dict[str, int]`: status별 전략 수 카운트
       - `get_portfolio_summary(session) -> dict`: 전체 전략 합산 summary (TradeRepository.summary)
       - `get_strategy_pnl(session, strategy_id) -> dict`: 개별 전략 summary
       - `get_system_health() -> dict`: JsonRuntimeStore.load()에서 mode, paused, paused_strategies, updated_at 추출
       - `get_open_positions(session) -> list[dict]`: TradeRepository.list_open 결과를 dict 리스트로 변환
       - `get_closed_trades(session, limit=50) -> list[dict]`: TradeRepository.list_closed 결과를 dict 리스트로 변환
    4. 기존 repo를 직접 import -- FastAPI 경유 금지 (연구 anti-pattern 준수)
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_dashboard.py -x -v</automated>
  </verify>
  <done>DashboardDataService의 모든 메서드가 기존 repo/store를 올바르게 래핑하고, 4개 테스트 통과</done>
</task>

<task type="auto">
  <name>Task 2: 멀티페이지 대시보드 (Lifecycle + Portfolio PnL + System Health)</name>
  <files>
    engine/interfaces/dashboard/pages/lifecycle_view.py,
    engine/interfaces/dashboard/pages/portfolio_pnl.py,
    engine/interfaces/dashboard/components/metrics_bar.py,
    engine/interfaces/dashboard/components/pnl_chart.py,
    engine/interfaces/dashboard/components/health_indicator.py,
    engine/interfaces/streamlit_dashboard.py
  </files>
  <action>
    1. `engine/interfaces/dashboard/components/metrics_bar.py`:
       - `render_metrics_bar(data: dict)`: st.columns(4)로 총 PnL, 승률, 평균 수익률, 총 거래 수 표시
       - 빈 데이터 시 "---" 표시

    2. `engine/interfaces/dashboard/components/pnl_chart.py`:
       - `render_pnl_chart(trades: list[dict])`: Plotly go.Scatter로 cumulative PnL 곡선 (fill="tozeroy")
       - exit_at 기준 정렬, 빈 데이터 시 st.info 표시

    3. `engine/interfaces/dashboard/components/health_indicator.py`:
       - `render_health(health: dict)`: st.columns(3)로 Mode, Paused 상태, 일시정지 전략 수 표시
       - `@st.fragment(run_every=30)` 데코레이터로 30초 자동 갱신

    4. `engine/interfaces/dashboard/pages/lifecycle_view.py`:
       - DashboardDataService로 전략 리스트 로드
       - status별 카운트를 st.metric으로 상단 표시 (draft/testing/paper/active/archived 5열)
       - 전체 전략을 st.dataframe으로 표시 (id, name, status, sharpe 컬럼)
       - `@st.fragment(run_every=30)` 으로 자동 갱신

    5. `engine/interfaces/dashboard/pages/portfolio_pnl.py`:
       - DashboardDataService + get_session()으로 데이터 로드
       - metrics_bar 컴포넌트로 상단 요약
       - pnl_chart 컴포넌트로 수익 곡선
       - 활성 포지션 st.dataframe (list_open)
       - 최근 거래 히스토리 st.dataframe (list_closed, limit=50)
       - `@st.fragment(run_every=30)` 으로 실시간 패널 자동 갱신

    6. `engine/interfaces/streamlit_dashboard.py` 리팩토링:
       - 기존 단일 페이지 코드를 멀티페이지로 전환
       - `st.set_page_config(page_title="Trading Dashboard", layout="wide")` 유지
       - `st.navigation([st.Page(...)])` 으로 3개 페이지 연결:
         - Lifecycle (lifecycle_view.py)
         - Portfolio PnL (portfolio_pnl.py)
         - System Health (health_indicator 컴포넌트를 페이지로 래핑)
       - 기존 _load_trades, _load_open_positions 함수 삭제 (DashboardDataService로 대체)
       - System Health 페이지에서 health_indicator 컴포넌트 렌더링

    주의: fragment 내부에 interactive widget 금지 (연구 pitfall 3 준수). display-only로 유지.
    주의: streamlit 미설치 시 ImportError 안내 유지 (기존 패턴).
  </action>
  <verify>
    <automated>.venv/bin/python -c "from engine.interfaces.dashboard.data_service import DashboardDataService; from engine.interfaces.dashboard.pages import lifecycle_view, portfolio_pnl; from engine.interfaces.dashboard.components import metrics_bar, pnl_chart, health_indicator; print('All imports OK')"</automated>
  </verify>
  <done>
    - streamlit_dashboard.py가 st.navigation으로 3페이지 (Lifecycle, Portfolio PnL, System Health) 제공
    - 각 페이지가 DashboardDataService를 통해 데이터 로드
    - 실시간 패널이 @st.fragment(run_every=30)으로 30초 자동 갱신
    - 모든 모듈 import 성공
  </done>
</task>

</tasks>

<verification>
1. `.venv/bin/python -m pytest tests/test_dashboard.py -x -v` -- 데이터 서비스 테스트 통과
2. `.venv/bin/python -c "from engine.interfaces.dashboard.data_service import DashboardDataService"` -- import 성공
3. `.venv/bin/python -m pytest` -- 전체 테스트 스위트 깨지지 않음
</verification>

<success_criteria>
- DashboardDataService가 LifecycleManager, TradeRepository, JsonRuntimeStore를 래핑하여 대시보드 전용 데이터 제공
- Lifecycle 페이지에서 status별 전략 현황 표시
- Portfolio PnL 페이지에서 수익 곡선 + 포지션 + 거래 히스토리 표시
- System Health에서 runtime 상태 30초 갱신
- 기존 테스트 스위트 미파괴
</success_criteria>

<output>
After completion, create `.planning/phases/08-monitoring-dashboard/08-01-SUMMARY.md`
</output>
